# Worklog

Append-only. One entry per task cycle or session: date (UTC), task id, what
happened, decisions and why, verification commands run, follow-ups. Newest
at the bottom.

---

- 2026-08-16 UTC - Approved spec after verifying all required sections are present and non-empty.
- 2026-08-16 UTC - Approved plan after verifying required sections, spec approval, and runnable task T1.
- 2026-08-16 UTC - T1 implemented and opened as PR #1. Added the bounded minimal config/state readers, loopback-only fleet API server, text-only local dashboard, failure isolation, and tests; kept full security boundaries and peer fan-out in their planned later tasks. Verification: `bun test` (80 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript checks passed). The local correctness/security review panel cleared the diff after the verifier rejected an out-of-scope config-traversal claim because T2 owns canonical-root validation. One shepherd pass found CI green, no threads or holds, and a clean merge state, but Codex is configured and its review of head `d12bde4cc985dca57444a896a99cfeb9a8d01a4e` is still in flight (👀); the next pass must wait for that exact-head verdict before merging.
- 2026-08-16 UTC - T1 merged as PR #1 (squash `a5e5a9d6de3cf82295a1917b5da9d9881beb2c18`). CI was green on head `d12bde4cc985dca57444a896a99cfeb9a8d01a4e`; Codex supplied an attributable clean +1 verdict on the single-head PR; there were no review threads, findings, holds, or parked minors. No post-merge verification was needed.
- 2026-08-16 UTC - T2 implemented and opened as held major PR #2. Added bounded validation for repository and peer configuration, literal loopback/tailnet binds, exact origins and GitHub URLs; canonical fixed-surface `.factory` path resolution with symlink containment; exact CORS allowlisting; and security regression coverage. Verification: `bun test` (201 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript checks passed). The local correctness/security panel cleared the diff after the verifier rejected both findings as invalid: `contains()` explicitly rejects `..` after `realpath`, and the resolver's internal errors preserve dangling-link/race distinctions before sanitization. One shepherd pass found CI green, a clean merge state, no threads or findings, and Codex review still in flight (👀) on single head `d53bba1ba5d8e050bb3b0292e0cec53d3afc6a0e`. T2 remains `[R]` with `hold: true`; after Codex's exact-head verdict it still requires human merge authority because it is major.
- 2026-08-16 UTC - T2 merged as PR #2 (squash `78543f479ab6970f491766c8e7e5a7ca323f5934`). CI passed on head `fd81f9fd8288c2ad4784acf833b36eea56ab0599`; Codex posted an attributable clean +1 verdict on that head. The resolved Codex P2 and P1 findings were fixed in `d0f79a6` and `fd81f9f`; the remaining verified FIFO local-writer DoS was correctly classified minor, dispositioned in its thread, and added to rolling batch T11 without a minors-only push. No refutations or late post-merge bot verdicts required records.
- 2026-08-16 UTC - T3 implemented and opened as PR #3. Added bounded recognized-driver-log selection, a fixed shell-free and timeout-bounded `lsof` probe, conservative exact-`tee` tristate classification, injected runner/clock seams, and liveness timestamps in repository snapshots. Verification: `bun test` (266 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript checks passed). The local correctness and security/tests reviewers returned clean findings, and the adversarial verifier independently cleared the diff. One shepherd pass found CI green, no threads, findings, or holds, and a clean merge state on head `7da710359fdd8aaa709dafc996302c70099ca198`, but Codex review is still in flight (👀); T3 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 UTC - T3 merged as PR #3 (squash `1ed708b3fae3876965661c3db9808142a9a7bf5f`). CI passed on head `3c85ed0864dcc16bc596ce28680f516295642719`; Codex posted a clean exact-head review comment, and no Claude review workflow is configured. The numeric log-sequence and ambiguous zero-padded suffix findings were fixed before merge. The remaining `.factory/logs` directory-swap TOCTOU concern was verifier-classified minor, dispositioned without a minors-only push, and appended to rolling batch T11. No refutations or late post-merge verdicts required records.
- 2026-08-16 UTC - T4 implemented and opened as PR #4. Added descriptor-validated bounded fixed-file reads, conservative approved-state parsing, exact top-level plan task/dependency parsing, explicit partial/unavailable warnings, runnable and status groupings, configured repository identity preservation, and regression fixtures. Verification: `bun test` (372 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript checks passed). The local correctness/security panel was CLEAR after the verifier rejected all proposed findings as refuted or out of scope. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, but Codex review is still in flight (👀) on head `d0cf28264192e8718922bf21a848835e93607943`; T4 remains `[R]` until an attributable exact-head verdict permits merge.
- 2026-08-16 UTC - T4 merged as PR #4 (squash `c4125a04f1bdd5dfd9a8fc988f836bd2a99e7f30`). CI passed on head `d0cf28264192e8718922bf21a848835e93607943`; Codex reviewed that exact head. Its fenced-Markdown task finding was verifier-classified minor, dispositioned on the thread, and appended to rolling batch T11 without a minors-only push. No refutations or late post-merge verdicts required records.
- 2026-08-16 UTC - T5 implemented and opened as PR #5. Added bounded exact-protocol open-question and recent-worklog readers, verbatim untrusted text preservation, explicit partial/unavailable warnings, independent snapshot aggregation, and regression fixtures. Verification: `bun test` (455 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript checks passed). The local correctness/security panel was CLEAR after the verifier rejected five self-refuting findings as invalid. One shepherd pass found CI green, no threads, holds, findings, or Claude workflow, and a clean merge state on head `86c132510246f178cf65c3fdab527130743de744`, but Codex review is still in flight (👀); T5 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 UTC - T5 merged as PR #5 (squash `4453c0d29d5eb7e0f9880997de9472ba18701c93`). CI passed on head `86c132510246f178cf65c3fdab527130743de744`; Codex reviewed that exact head, and no Claude workflow is configured. Its answered-entry validation finding was verifier-classified invalid because answered entries are historical records excluded from the open-question reader; the disposition remains in its unresolved [thread](https://github.com/MrZoller/factory-ui/pull/5#discussion_r3791265376). No parked minors or late post-merge verdicts required records.
- 2026-08-16 UTC - T6 implemented and opened as PR #6. Added bounded deterministic driver/cycle/shepherd log selection, byte/line/line-length-bounded verbatim narration, source-derived start/activity/duration and per-source/overall age data, and exact selected-driver identity reuse for liveness. Verification: `bun test` (561 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript passed). The local panel was CLEAR after fixing its confirmed medium warning-truncation finding and re-running both review lenses plus the verifier successfully. One shepherd pass remains before merge eligibility is known.
- 2026-08-16 UTC - T6 PR #6 shepherd pass found CI failing because the replacement-identity test assumed an unlinked inode would not be reused. Made the fixture deterministic by renaming the selected file before creating its replacement, then pushed head `e5f1019`; verification remained `bun test` (561 pass, 0 fail) and `bun run lint` (passed). The PR remains `[R]`: CI and Codex must review the new head before a later shepherd pass can merge it; there are no threads or holds, and the branch is mergeable.
- 2026-08-16 UTC - T6 post-pass observation: CI is green on replacement-test fix head `e5f1019`; only an attributable Codex verdict on that head remains before the next shepherd can reassess merge eligibility.
- 2026-08-16 UTC - T6 merged as PR #6 (squash `13a2936`). CI passed on head `69619792bbf77124b5816eec65ffe3391fd8f039`; Codex posted a clean exact-head review comment after the UTC timestamp and deterministic `lsof` fixture findings were fixed and their threads resolved. No Claude review workflow is configured, no holds, parked minors, refutations, or late bot verdicts required records.
- 2026-08-16 UTC - T7 implemented and opened as PR #7. Finalized schema-versioned fleet and per-repository APIs with complete bounded factory state, plan groups, questions, worklog, narration, timing, liveness, warnings, generation/source timestamps, strict one-decode repository selection before I/O, failure isolation, CORS, security headers, and read-only/non-disclosure integration coverage. Verification: `bun test` (566 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local correctness/security panel was CLEAR after the verifier rejected all six proposed security/test findings using config validation, generic catch boundaries, disclosure tests, and required `Vary: Origin` behavior. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean merge state on head `9c42e30c02af0710fa7ebcb5afd8846739b86b0d`, but Codex review is still in flight (👀); T7 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 UTC - T7 merged as PR #7 (squash `508f1dd55d3244b0a09795d21acfbec6a64b2e5b`). CI passed on head `9c42e30c02af0710fa7ebcb5afd8846739b86b0d`; Codex reviewed that exact head, and no Claude review workflow is configured. Its configured-GitHub-URL finding was verifier-classified invalid because PR-link rendering is explicitly T8 scope and T7 intentionally exposes `RepositorySource`, not `RepositoryConfig`; the disposition remains in its unresolved [thread](https://github.com/MrZoller/factory-ui/pull/7#discussion_r3791567605). No parked minors or late bot verdicts required records.
- 2026-08-16 UTC - T8 implemented and opened as held major PR #8. Added a responsive text-only repository dashboard for current state, all work groups, questions, worklog, narration, timing, liveness, warnings, and source age; generated PR links only from validated GitHub configuration and revalidated them before safe external linking; and added browser-level hostile-content plus static security-header coverage. Verification: `bun test` (574 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript passed). The local correctness/security panel was CLEAR after the verifier rejected all three proposed findings as contradicted by the restrictive no-inline CSP and explicit HTTPS/server-side/browser-side URL validation. One shepherd pass found CI green, zero threads or findings, no Claude workflow, and a clean merge state on head `d183d80916c71746196bb641f57c27343dd135bb`; Codex is configured and still in flight (👀). T8 remains `[R]` with `hold: true`: it requires both an attributable Codex verdict on that head and human merge authority because it is major.
- 2026-08-16 UTC - T8 merged as PR #8 (squash `0def0cfd6f43b9ce4375817bad082a503510bae9`) under human authority for the held major. CI passed on final head `00f75c25f5053d460bc975afb09fd4f7910cdc60`; no Claude review workflow is configured. All six Codex findings were fixed and their threads resolved in `ca03267`, `dbe8eae`, and `00f75c2`; there were no parked minors, refutations, or post-merge bot verdicts. The final Codex +1 appeared before the merge but was unattributable on this multi-head PR; it did not alter the human-authorized merge or require follow-up.
- 2026-08-16 UTC - T9 implemented and opened as PR #9. Added direct browser peer fan-out with immediate machine slots, a fixed five-second timeout and four-request concurrency limit, bounded streamed response parsing and validation, independent `UNREACHABLE` failure states, non-recursive peer discovery, and stale-refresh protection. Verification: `bun test` (582 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local correctness/security panel was CLEAR after the verifier rejected all five proposed correctness findings as contradicted by JavaScript execution semantics, generation guards, prior origin validation, or intentional shared styling. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean merge state on head `0710b1fd1f07448fd04ea04d79206d7f42fc2e02`, but Codex review is still in flight (👀); T9 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 UTC - T9 merged as PR #9 (squash `1ec4a66`). CI passed on head `0710b1fd1f07448fd04ea04d79206d7f42fc2e02`; Codex reviewed that exact head, and no Claude review workflow is configured. Its nested-payload and stale-worker findings were verifier-classified invalid: available reader data and peer collections are bounded and validated, and stale generations return before starting peer workers. The dispositions remain in unresolved [nested-payload](https://github.com/MrZoller/factory-ui/pull/9#discussion_r3791737506) and [stale-worker](https://github.com/MrZoller/factory-ui/pull/9#discussion_r3791737509) threads. No parked minors or late post-merge bot verdicts required records.
- 2026-08-16 UTC - T10 implemented and opened as PR #10. Added the exact `serve --config <path>` launch contract with fail-before-listen configuration loading, package scripts, ignored machine-local configuration and a committed credential-free example, plus the mini/macbook/legion operations and troubleshooting runbook. Verification: `bun test` (589 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), `git check-ignore factory-ui.config.json factory-ui.config.mini.json` with a non-ignored example check (passed), and `bun run serve --config does-not-exist.json` (expected pre-listen rejection). The local panel was CLEAR after documenting and testing the 128-question limit and distinct machine clone roots. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, but Codex is configured and still in flight (👀) on single head `07981f299830a07b0de732cf84ef6460f202cea8`; T10 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 UTC - T10 merged as PR #10 (squash `e6dd7c5682c93ae50174709fa891b8725b373d8d`). CI passed on final head `8708c9080e9d506e350ce5ae2e72b0f355164d1b`; Codex posted a clean exact-head review comment, and no Claude review workflow is configured. The prior MagicDNS-origin finding was fixed in `8708c90` and its thread resolved. No parked minors, refutations, or late post-merge verdicts required records.
- 2026-08-16 UTC - T12 implemented and opened as PR #11. Added the root MIT license for Chris Zoller, ignored root `tmp-*` scratch paths, removed existing scratch litter, and guaranteed the repository-root hostile liveness fixture is removed through `try`/`finally`; no current test creates the historical `tmp-logs-debug-*` or `tmp-test-oversized-*` prefixes. Verification: `bun test` (589 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript passed). Both local review lenses returned CLEAN and the verifier returned CLEAR. One shepherd pass found CI green on head `5f4793068d93989d3b00707115ca81836439faae`, no threads, findings, holds, or Claude workflow, but Codex is configured and still in flight (👀); T12 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 UTC - T12 merged as PR #11 (squash `eec4c32`). CI passed on head `5f4793068d93989d3b00707115ca81836439faae`; Codex supplied an attributable clean +1 verdict on the single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, refutations, or late verdicts requiring records. The current rolling minor batch T11 remains intentionally blocked; no runnable task remains.
- 2026-08-16 15:43 UTC - T13 implemented and opened as PR #13. Added mixed legacy date-only and zero-padded `HH:MM` UTC worklog stamp parsing, an optional API time field, text-only timed headings, documentation, and parser/browser regressions for valid edges and malformed times. Verification: `bun test` (604 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript passed). The local correctness/security panel and adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean merge state on head `16907bf41a7e4f72b706f029abe504a4a1e0ff6c`, but Codex review is still in flight (👀); T13 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 15:44 UTC - T13 merged as PR #13 (squash `09521d02bec266898c30778e175d3b1d3df1969f`). CI passed on head `16907bf41a7e4f72b706f029abe504a4a1e0ff6c`; Codex supplied an attributable clean +1 verdict on this single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, refutations, or late verdicts requiring records. No runnable task remains; rolling minor batch T11 remains intentionally blocked.
- 2026-08-16 16:33 UTC - T14 implemented and opened as PR #14. Added a local-first fleet summary with per-machine liveness, current work, hold, question, and age aggregation; accessible hash-persisted machine tabs; fixed unavailable peer views; text-only hostile-string handling; and globally unique local/peer machine names for unambiguous hash routing. Verification: `bun test` (612 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local panel was CLEAR after fixing all three verified minors (machine-name collisions plus STOPPED and multi-repository aggregation coverage) and re-running both lenses CLEAN. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a mergeable head `23bddc423b30ccf821e3fffbb222e85767324525`, but Codex review is still in flight (👀); T14 remains `[R]` until an attributable exact-head verdict permits merge.
- 2026-08-16 16:38 UTC - T14 merged as PR #14 (squash `cdd8dea`). CI passed on head `23bddc423b30ccf821e3fffbb222e85767324525`; Codex supplied an attributable clean +1 verdict on the single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, refutations, or late verdicts requiring records.
- 2026-08-16 17:04 UTC - T15 implemented and opened as PR #15. Added a fixed, descriptor-safe 16 KiB routing target and bounded schema-versioned reader; exposed routing independently in snapshots and API responses; rendered the first available repository routing as a hostile-safe per-machine strip with fixed provider classes; and documented the read surface and schema. Verification: `bun test` (658 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a mergeable single head `6579b04a612e48040111b371c25b64158a6e7f63`, but Codex is configured and still in flight (👀); T15 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 17:16 UTC - T15 merged as PR #15 (squash `ebc5b6d9a1099dcf536ba5e24df9fead38953447`). CI passed on final head `1309431748d4b994b667c5b602ffa6398b5238e2`; Codex reviewed that exact head, and no Claude review workflow is configured. The earlier peer-compatibility finding was fixed in `1309431748d4b994b667c5b602ffa6398b5238e2` and resolved. Its timestamp-precision finding was verifier-classified invalid because the sole routing producer emits whole-second UTC with `date -u +%Y-%m-%dT%H:%M:%SZ`; the disposition remains in its unresolved [thread](https://github.com/MrZoller/factory-ui/pull/15#discussion_r3792306769). No parked minors or late post-merge verdicts required records.
- 2026-08-16 17:33 UTC - T16 implemented and opened as PR #16. Updated the liveness parser to ignore recognized `lsof -Fpc` file-set fields only after a strict process/command pair, while preserving fail-closed ordering and malformed-output behavior; added real captured `tee`/`tail` parser and probe coverage. Verification: `bun test src/liveness.test.ts` (67 pass, 0 fail), `bun test` (660 pass, 0 fail), and `bun run lint` (Prettier and strict TypeScript passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean merge state on head `6ce7a3d576531aa040d5080de6002a9faf60abe1`, but Codex is configured and still in flight (👀); T16 remains `[R]` until an attributable verdict on that head permits merge.
- 2026-08-16 17:41 UTC - T16 merged as PR #16 (squash `0bc19d352753865c27019e9b1540a7d052606bd7`). CI passed on head `6ce7a3d576531aa040d5080de6002a9faf60abe1`; Codex reviewed that exact head, and no Claude review workflow is configured. Its file-set-before-`f` finding was verifier-classified invalid: ignored `n` metadata never adds a command, so `RUNNING` still requires `ctee` exactly as before; the disposition remains in its unresolved [thread](https://github.com/MrZoller/factory-ui/pull/16#discussion_r3792371656). No parked minors or late post-merge verdicts required records.
- 2026-08-16 18:01 UTC - T17 implemented and opened as PR #17. Added bounded task PR and `Fixes #N` parsing, server-derived repository/branch/task links from validated GitHub configuration, browser-side URL revalidation with inert text fallback, and positive/hostile link coverage. Verification: `bun test` (669 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local panel was CLEAR after adding the verifier-requested leading `-` and `/` branch rejection tests and re-running both lenses plus the verifier CLEAN. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean mergeable head `4f89d9c885ec98c1e6fd47c5fa6a8eded87715f7`, but Codex is configured and still in flight (👀); T17 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 18:10 UTC - T17 merged as PR #17 (squash `2b0f158a941f414b36902056da3ba005d5946e34`). CI passed on head `4f89d9c885ec98c1e6fd47c5fa6a8eded87715f7`; Codex reviewed that exact head, and no Claude review workflow is configured. Two Codex findings were verifier-classified minor and parked in T11: [trailing issue-reference punctuation](https://github.com/MrZoller/factory-ui/pull/17#discussion_r3792466239) and [empty `pr:` metadata](https://github.com/MrZoller/factory-ui/pull/17#discussion_r3792466244). Their disposition replies are the durable records; both threads remain open until the batch fix lands. No refutations or late verdicts required records.
- 2026-08-16 18:39 UTC - T18 implemented and opened as PR #18. Added per-machine repository summary strips and accessible repository sub-tabs, two-key machine/repository hash persistence and fallback, keyboard navigation that survives peer fan-out reinstallation, unavailable-machine isolation, responsive styling, and hostile-text-safe rendering. Verification: `bun test` (675 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local panel initially confirmed one blocking arrow-key/hash persistence defect and two minor test gaps; all were fixed, then both re-review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean mergeable head `01fcab94877e46b33d4cea0964450ea90ac94d6e`, but Codex is configured and still in flight (👀); T18 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 18:44 UTC - T18 merged as PR #18 (squash `b0b63c135e4ca35d57f2356cc8cc561477a6c20a`). CI passed on head `01fcab94877e46b33d4cea0964450ea90ac94d6e`; Codex supplied an attributable clean +1 verdict on this single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations. Late-verdict sweep found PR #17's recorded count unchanged (1) within its 30-minute window.
- 2026-08-16 19:04 UTC - T19 implemented and opened as PR #19. Added visible-only automatic and manual refresh, bounded `?refresh=` pacing, in-flight and stale-response guards, last-good snapshot retention with capped exponential backoff, local snapshot-age ticking, and machine/repository/hash/scroll preservation; added deterministic browser coverage for the complete refresh state machine. Verification: `bun test` (681 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean merge state on head `b6f74e19e4b489b1ffd5fa2fa1f98f2f11518a1b`, but Codex is configured and still in flight (👀); T19 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 19:07 UTC - T19 merged as PR #19 (squash `3f09e1d`). CI passed on head `b6f74e19e4b489b1ffd5fa2fa1f98f2f11518a1b`; Codex reviewed that exact head with no findings, no Claude review workflow is configured, and there were no review threads, holds, parked minors, or refutations. Late-verdict sweep found PR #17's recorded count unchanged and expired its marker; PR #18 remains within its 30-minute window with recorded count 0.
- 2026-08-16 19:08 UTC - No runnable task remains after reconciliation and sync: T1-T10 and T12-T19 are complete, while rolling review-minor batch T11 remains intentionally blocked. Set the factory phase to `idle`. Verification: inspected `.factory/questions.md` (no answered or open questions), `.factory/plan.md`, and `.factory/state.json`; no code or test commands were required.
- 2026-08-16 19:39 UTC - T20 implemented and opened as PR #20. Replaced the perpetually ticking snapshot age with a quiet absolute update time while fresh, added highlighted stale states for refresh failure, pause, and peer timeout, and applied the active refresh interval to per-machine age signals. The task was reclassified from trivial to standard after growing to 211 changed lines. Verification: `bun test` (685 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and the local correctness/security panel plus adversarial verifier returned CLEAR after the immediate stale-render finding was fixed. The age-only reason ambiguity is parked in T11. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, but Codex review is still in flight (👀) on head `7a65968f`; T20 remains `[R]` until an attributable verdict permits merge.
- 2026-08-16 19:44 UTC - T20 merged as PR #20 (squash `9c0c7bd`). CI passed on head `7a65968fa45d46b8a0616c2681972c582012da12`; Codex reviewed that exact head. Its two verified findings were minor: the age-only reason was already parked in T11, and a rare overlapping-load peer-timeout status race was added to T11. Both threads have disposition replies; no Claude review workflow is configured, no holds or refutations remain. Late-verdict sweep found PRs #18 and #19 unchanged beyond the 30-minute window and expired both markers.
- 2026-08-16 20:03 UTC - T21 implemented and opened as PR #21. Added a documented spacing/type/radius/light-dark token system, explicit 12-column repository panel spans with 1200/800 responsive collapse, compact empty cards, shared fixed task metadata columns, and unified dashboard typography/status styling without changing source-text or CSP behavior. Verification: `bun test` (685 pass, 0 fail) and `bun run lint` (Prettier and strict TypeScript passed); both local review rounds and their adversarial verifiers returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean mergeable head `7fe9ae5e32c0add151b8951bf97659dbc17ca310`, but Codex is configured and still in flight (👀); T21 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 20:10 UTC - T21 merged as PR #21 (squash `ebc6addbfaf50757729bfa39c35d9dabcc6ee6b1`). CI passed on head `7fe9ae5e32c0add151b8951bf97659dbc17ca310`; Codex reviewed that exact head, and no Claude review workflow is configured. Its long unbroken hostname clipping finding was verifier-classified minor, dispositioned in its [thread](https://github.com/MrZoller/factory-ui/pull/21#discussion_r3792704477), and appended to rolling batch T11 without a minors-only push. No refutations or late verdicts required records.
- 2026-08-16 20:20 UTC - T22 implemented and opened as PR #22. Added a restrained Factory wordmark with a decorative inline SVG, light/dark gradient text and solid fallback, and a secondary text-only machine subtitle; added served-HTML regressions for the literal heading, decorative mark, inline handlers, and external asset URLs. Reclassified the task from trivial to standard after the implementation reached 80 additions and 5 removals. Verification: `bun test` (685 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed); both local review lenses and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean mergeable head `e0da8fd09aac9451eeae4c6f4ceb505a3c1c2dc3`, but Codex is configured and its 👀 signal shows review still in flight; T22 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 20:22 UTC - T22 merged as PR #22 (squash `aa77dc3e5ae05e469b2641836df48162a829fcf6`). CI passed on head `e0da8fd09aac9451eeae4c6f4ceb505a3c1c2dc3`; Codex supplied an attributable clean +1 verdict on this single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, refutations, or late verdicts requiring records.
- 2026-08-16 20:52 UTC - T23 implemented and opened as PR #23. Added the fixed descriptor-safe 64 KiB costs target and bounded schema-versioned reader; exposed costs independently in repository snapshots and API contracts; rendered validated per-task subscription/metered usage plus repository, unattributed, and machine totals; and documented the read surface and schema. Verification: `bun test` (716 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and the local correctness/security panel plus adversarial verifier returned CLEAR after adding fetch-path and invalid-UTF-8 regressions for both confirmed low test gaps. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean merge state on head `db8803d4c712176c07091f47630824835c56d0fa`, but Codex review is still in flight (👀); T23 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 21:08 UTC - T23 merged as PR #23 (squash `ba5e29f`). CI passed on final head `497c1789427303150626cd19c79da6e12943706e`; Codex reviewed that exact head, and no Claude review workflow is configured. Its non-USD-currency and aggregate-overflow findings were independently verifier-classified minor, dispositioned in their [currency](https://github.com/MrZoller/factory-ui/pull/23#discussion_r3792813452) and [overflow](https://github.com/MrZoller/factory-ui/pull/23#discussion_r3792813456) threads, and appended to rolling batch T11 without a minors-only push. Late-verdict sweep found PRs #20, #21, and #22 unchanged beyond the 30-minute window and expired their markers.
- 2026-08-16 21:28 UTC - T24 implemented and opened as PR #24. Added the `/how` live factory diagram with six guarded phases, approval and major-hold branches, all required agent roles, provider-classed routing and tracked USD model costs, accessible hash-persisted machine tabs, bounded peer failure isolation, contained responsive scrolling, and text-only rendering under the existing CSP. Verification: `bun test` (722 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local panel confirmed and fixed absent-model cost semantics plus two acceptance-test gaps; both re-review lenses and the adversarial verifier then returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-16 21:30 UTC - T24 PR #24 shepherd pass found CI green, no threads, holds, findings, or Claude workflow, and a clean merge state on head `ca1b0b4ef82b8ab5896dc8959891277bf10bd03a`. Codex is configured and its PR-body 👀 shows exact-head review is still in flight, so T24 remains `[R]`; the next shepherd pass must wait for an attributable verdict before merging.
- 2026-08-16 21:39 UTC - T24 merged as PR #24 (squash `6fc95fc5ede4baf7d96160445784c0144dc31860`). CI passed on head `ca1b0b4ef82b8ab5896dc8959891277bf10bd03a`; Codex reviewed that exact head, and no Claude review workflow is configured. Its focus/scroll preservation and subscription-lane labeling findings were verifier-classified minor, dispositioned in their [focus/scroll](https://github.com/MrZoller/factory-ui/pull/24#discussion_r3792869443) and [subscription](https://github.com/MrZoller/factory-ui/pull/24#discussion_r3792869444) threads, and appended to rolling batch T11 without a minors-only push. No refutations or late bot verdicts required records.
- 2026-08-16 21:41 UTC - No runnable task remains after reconciliation and sync: T1-T10 and T12-T24 are complete, while rolling review-minor batch T11 remains intentionally blocked. Set the factory phase to `idle`. Verification: inspected `.factory/questions.md` (no answered or open questions), `.factory/plan.md`, and `.factory/state.json`; no code or test commands were required.
- 2026-08-16 22:05 UTC - T11 implemented and opened as PR #25. Resolved the twelve parked review minors spanning descriptor-safe state/log reads, plan metadata parsing, snapshot freshness and peer-generation state, safe cost currency/aggregation, long machine headings, and `/how` rerender behavior. Verification: `bun test` (735 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed); both local review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no labels or review threads, no Claude workflow, and a clean mergeable head `ee3fcb26704cc4ce4e9f2136fca70c41f5af4580`, but Codex review is still in flight (👀); T11 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 22:13 UTC - T11 merged as PR #25 (squash `05c7115dbc8938a2741b3dfaa4559f1b0ed49953`). CI passed on head `ee3fcb26704cc4ce4e9f2136fca70c41f5af4580`; Codex reviewed that exact head, and no Claude review workflow is configured. Two Codex findings were independently verifier-classified minor — initial logs-directory handle cleanup and swap-and-restore liveness-probe binding — dispositioned in their [handle](https://github.com/MrZoller/factory-ui/pull/25#discussion_r3792935275) and [probe](https://github.com/MrZoller/factory-ui/pull/25#discussion_r3792935276) threads, and appended to rolling batch T11 without a minors-only push. Late-verdict sweep found PR #24's recorded bot count unchanged and expired its marker. No refutations required records.
- 2026-08-16 22:27 UTC - T26 implemented and opened as PR #26. Bound default timeout and interval dependencies to the browser global in `startDashboard`, `loadFleet`, and `fetchPeerFleet`; added receiver-checking default-path coverage for the immediate local fetch, age ticker, peer fetch, and refresh re-arm; and made test-global restoration safe when auto-import fails. Verification: `bun test src/public/app.test.ts` (55 pass, 0 fail), `bun test` (736 pass, 0 fail), and `bun run lint` (Prettier and strict TypeScript passed). The local correctness/security panel and adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, labels, holds, or Claude workflow, and a clean mergeable head `a2213d707cecdf55b6fd906348102760281cb7ed`, but Codex is configured and still in flight (👀); T26 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-16 22:29 UTC - T26 merged as PR #26 (squash `05cd33d`). CI passed on head `a2213d707cecdf55b6fd906348102760281cb7ed`; Codex posted an attributable clean +1 on this single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations. Late-verdict sweep found PR #25's recorded bot count unchanged; its marker remains within the 30-minute window.
- 2026-08-16 22:50 UTC - T27 opened PR #27 for the scannable recent-worklog panel: newest-first six-entry view and toggle, date/time headline rows, deterministic event chips, safe GitHub/code highlights, proportional body text, and verbatim details fallback. Verification: `bun test` passed (736 tests); `bun run lint` passed (Prettier and strict TypeScript); correctness plus security/tests panel cleared after the confirmed no-remote `<code>` fallback was fixed and re-reviewed. One shepherd pass confirmed CI green, no review threads, no hold, and a mergeable exact head `4fe20c3a5379179981805bc6e8ec88c2f6a6a1dc`; PR remains in review awaiting the configured Codex bot's completed verdict (only an in-flight eyes reaction was present), so it was not merged this cycle.
- 2026-08-16 23:43 UTC - T27 merged as PR #27 (squash `9edd0ac790b27069fafb1741d1162eba0e867cee`). CI passed on head `4ad66727a79e2758e197b6a1456b56735c1f5d63`; Codex reviewed that exact head, and no Claude review workflow is configured. Its verdict-pending event-chip finding was verifier-classified minor, dispositioned in its [thread](https://github.com/MrZoller/factory-ui/pull/27#discussion_r3793096723), and parked in rolling batch T31 without a minors-only push. No refutations required records.
- 2026-08-17 00:11 UTC - T28 implemented and opened as PR #28. Added bounded source-line excerpts to reader warnings; grouped and sorted warning rows with locations, duplicate counts, complete plain-English explanations, hostile-safe excerpts, and current-snapshot/open-state semantics; and added reader, snapshot, and browser regressions. Verification: `bun test` (743 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, holds, or Claude workflow, and a clean mergeable head `6f4e01c428a7fa53d3f0f5769dceab81b504f389`, but Codex is configured and still in flight (👀); T28 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-17 00:19 UTC - T28 merged as PR #28 (squash `eee037394d67e19b44c09de2efe4fd69973f332c`). CI passed on final head `6f4e01c428a7fa53d3f0f5769dceab81b504f389`; Codex reviewed that exact head, and no Claude review workflow is configured. Its own-property warning-explanation finding was independently verifier-classified minor, dispositioned in its [thread](https://github.com/MrZoller/factory-ui/pull/28#discussion_r3793171581), and parked in rolling batch T31 without a minors-only push. Late-verdict sweep found PR #27's recorded bot count unchanged and expired its marker. No refutations required records.
- 2026-08-17 00:40 UTC - T29 implemented and opened as PR #29. Applied the active snapshot-refresh freshness rule to the Driver activity liveness check, omitted fresh check timestamps, retained Source age, and highlighted stale or missing check times. The local panel confirmed and fixed the server's generated-before-checked timestamp ordering defect plus the fresh-case regression assertion; the one allowed re-panel was CLEAR after its remaining low test assertion issue was fixed. Verification: `bun test` (746 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and final `bun test src/public/app.test.ts` (62 pass, 0 fail). One shepherd pass found CI green, no threads, labels, holds, findings, or Claude workflow, and a clean mergeable head `dee20c6988382643cd7c63777f59b1e5f4d9a166`, but Codex is configured and still in flight (👀); T29 remains `[R]` until an attributable verdict on that exact head permits merge.
- 2026-08-17 00:42 UTC - T29 merged as PR #29 (squash `eb9ae52ac0aa9c112288f47c7a99c44080962a00`). CI passed on head `dee20c6988382643cd7c63777f59b1e5f4d9a166`; Codex supplied an attributable clean +1 verdict on this single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations. Late-verdict sweep found PR #28's recorded bot-review/comment count unchanged (1) within its 30-minute window.
- 2026-08-17 00:45 UTC - T30 reclassified from trivial to standard before implementation because adding four API fields, a new fixed read target, browser rendering, styling, and server/browser security regressions is a public-interface change expected to exceed the trivial line budget. Existing approved spec and plan gates satisfy the standard-task gate; scope and acceptance are unchanged.
- 2026-08-17 01:00 UTC - T30 implemented and opened as PR #30. Added bounded, fixed-path readability gating and API fields for GitHub `HEAD` links to spec, plan, worklog, and questions; rendered exact client-validated links under Current and on plan-backed, worklog, and questions headings with inert text fallback; and documented the expanded read surface. Verification: `bun test` (750 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). The local correctness reviewer was CLEAN; security/tests found one low hostile-URL API coverage gap, which the verifier confirmed as minor, then the fix re-panel and verifier returned CLEAR.
- 2026-08-17 01:02 UTC - T30 PR #30 shepherd pass found CI green, no labels, review threads, comments, holds, findings, or Claude review workflow, and a clean mergeable head `312ca7cddde4c48167d760b6b707461f7fc55619`. Codex is configured and its PR-body 👀 shows exact-head review is still in flight; no attributable verdict exists yet, so T30 remains `[R]` for the next shepherd pass.
- 2026-08-17 01:28 UTC - T30 merged as PR #30 (squash `8964502`). CI passed on final head `312ca7cddde4c48167d760b6b707461f7fc55619`; Codex supplied an attributable clean +1 verdict on the single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations. Late-verdict sweep found PR #29's recorded bot-review/comment count unchanged (0) and expired its marker; PR #30's late-verdict cursor is initialized at 0.
- 2026-08-17 01:59 UTC - T32 implemented and opened as PR #31. Added bounded additive routing model metadata, guarded subscription-lane notional list pricing kept separate from real metered cost at task/repository/machine levels, and model names, token limits, and per-million prices on `/how`; legacy routing remains compatible and hostile data stays text-only. Verification: `bun test` (761 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, labels, holds, or Claude workflow, and exact head `5902c553a0ef386d03ee33bf211932dd27e6799c`; Codex is configured and still in flight (👀), so T32 remains `[R]` until an attributable verdict permits merge.
- 2026-08-17 02:07 UTC - T32 merged as PR #31 (squash `3ac37d2a10d9da5f54e68fe6be4566db1987173b`). CI passed on head `5902c553a0ef386d03ee33bf211932dd27e6799c`; Codex reviewed that exact head, and no Claude review workflow is configured. Its [notional aggregate](https://github.com/MrZoller/factory-ui/pull/31#discussion_r3793410989) and [narrow cost-label](https://github.com/MrZoller/factory-ui/pull/31#discussion_r3793410991) findings were independently verifier-classified invalid and refuted in their unresolved threads: the former contradicts T32's deliberate omission of wholly unpriced usage, and the latter misidentifies overflow-visible flex layout as clipping. No parked minors or late bot verdicts required records.
- 2026-08-17 02:15 UTC - T33 reclassified from trivial to standard after implementation and acceptance tests exceeded the trivial line budget. The approved spec and plan satisfy the standard gate; scope and acceptance remain unchanged.
- 2026-08-17 02:24 UTC - T33 implemented and opened as PR #32. Machine summary rows now sum finite metered and notional totals from repositories with available cost data, label partial coverage as `k of n repos`, and identify unavailable repositories in tooltips while retaining `Unavailable` for no contributors or non-finite sums. Verification: `bun test` (763 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR.
- 2026-08-17 02:26 UTC - T33 PR #32 shepherd pass found CI green, no labels, review threads, comments, findings, holds, or Claude workflow, and a clean mergeable exact head `f7054a51122a96403cd541ba0630bee80445a3fb`. Codex is configured and its PR-body 👀 shows exact-head review is still in flight; no attributable verdict exists yet, so T33 remains `[R]` for the next shepherd pass.
- 2026-08-17 02:28 UTC - T33 merged as PR #32 (squash `705ad8c19a087906fc31843a9207d54d8ed504e5`). CI passed on head `f7054a51122a96403cd541ba0630bee80445a3fb`; Codex supplied an attributable clean +1 verdict on the single-head PR. There were no review threads, findings, holds, parked minors, or refutations; no Claude review workflow is configured. Late-verdict markers for PRs #30 and #31 matched their recorded bot verdict counts; the expired #30 marker was removed and #31 remains within its 30-minute sweep window.
- 2026-08-17 02:55 UTC - T36 implemented and opened as PR #33. Added a page-lifetime disclosure store keyed by document, machine, and repository so worklog expansion, raw-entry details, and warning-panel choices survive full refreshes and peer reinstalls; stale worklog keys are pruned. Reclassified T36 from trivial to standard after implementation and acceptance coverage exceeded the trivial line budget; existing approved gates remained satisfied. Verification: `bun test` (767 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local panel confirmed one minor machine-isolation test gap; after adding that regression, both re-review lenses returned CLEAN and the verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, labels, holds, or Claude workflow, and a clean mergeable exact head `9169a538c7b0d51f83ac12bb6d9b1ecf29f071e2`; Codex is configured and still in flight (👀), so T36 remains `[R]` until an attributable verdict permits merge.
- 2026-08-17 03:08 UTC - T36 merged as PR #33 (squash `7933e4f2aa86df937a533c9e53f0233ca77da3c9`). CI passed on head `9169a538c7b0d51f83ac12bb6d9b1ecf29f071e2`; Codex reviewed that exact head, and no Claude review workflow is configured. Its stale-empty-worklog and automatic-warnings-default findings were independently verifier-classified minor, dispositioned in their [worklog](https://github.com/MrZoller/factory-ui/pull/33#discussion_r3793550099) and [warnings](https://github.com/MrZoller/factory-ui/pull/33#discussion_r3793550101) threads, and parked in rolling batch T31 without a minors-only push. Late-verdict sweep found PR #31 and #32 counts unchanged (1 and 0); both markers remain within the 30-minute window.
- 2026-08-17 03:25 UTC - T35 implemented and opened as PR #34. Completed tasks now sort known merge times newest-first with numeric task-id fallback, show the newest eight by default, preserve the per-repository expansion choice across refreshes, and use a bounded keyboard-focusable scroll region when expanded; other task groups retain plan order. Verification: `bun test` (770 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). The local panel confirmed one minor CSS-regression coverage gap; after adding the style assertion, both re-review lenses returned CLEAN and the verifier returned CLEAR. One shepherd pass found CI green, no threads, findings, labels, holds, or Claude workflow, and a clean mergeable exact head `63dfd0168a6a6820784ed7e145eb7906d6270a9a`; Codex is configured and still in flight (👀), so T35 remains `[R]` until an attributable verdict permits merge.
- 2026-08-17 03:38 UTC - T35 merged as PR #34 (squash `69b7706`). CI passed on final head `18f454e99b3bf1802b8fe259f75d3a2ffa7f0ef7`; Codex reviewed that exact head, and no Claude review workflow is configured. Its refresh-only completed-disclosure finding and high task-ID fallback-sort finding were independently verifier-classified minor, dispositioned in their [disclosure](https://github.com/MrZoller/factory-ui/pull/34#discussion_r3793627151) and [task-ID](https://github.com/MrZoller/factory-ui/pull/34#discussion_r3793643311) threads, and parked in rolling batch T31 without a minors-only push. T31 reached five parked minors and is now runnable as the next batch task. No refutations or late post-merge verdicts required records.
- 2026-08-17 03:55 UTC - T31 implemented and opened as PR #35. Resolved five parked dashboard review minors covering canonical review-wait wording, own-property warning explanations, disclosure-state cleanup and user-choice persistence, and exact arbitrary-length task-ID sorting. Reclassified T31 from trivial to standard because the implementation plus regression coverage exceeded the trivial size budget; approved standard gates were already satisfied. Verification: `bun test` (775 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR.
- 2026-08-17 03:56 UTC - T31 PR #35 shepherd pass found CI green on exact head `584540338e8afa3413f29d121ea9ac228760041d`, no labels, holds, review threads, comments, or Claude review workflow, and a clean mergeable branch. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable exact-head verdict exists yet, so T31 remains `[R]` for the next shepherd pass.
- 2026-08-17 03:58 UTC - T31 merged as PR #35 (squash `8eacfb235c42a75c43dc3458a545d1e3afa2ea51`). CI passed on final head `584540338e8afa3413f29d121ea9ac228760041d`; Codex supplied an attributable clean +1 verdict on the single-head PR. There were no review threads, findings, holds, parked minors, or refutations; no Claude review workflow is configured. A late-verdict marker was recorded at merge with zero counted bot reviews/comments.
- 2026-08-17 04:22 UTC - T37 implemented and opened as PR #36. Replaced task lists with accessible six-column semantic tables and sticky headers in bounded scroll regions, added protocol size guidance with a prototype-safe fallback, made Completed full-width while preserving its disclosure state, and compacted empty Open Questions. Verification: `bun test` (779 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). The local panel confirmed one minor prototype-chain tooltip lookup; it was fixed with `Object.hasOwn` plus a regression, and the required re-panel and adversarial verifier returned CLEAR.
- 2026-08-17 04:24 UTC - T37 PR #36 shepherd pass found CI green on exact head `7c02fd6ae6eab63c093ae3943f36233c4a024770`, no labels, holds, review threads, comments, or Claude review workflow, and a clean mergeable branch. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable exact-head verdict exists yet, so T37 remains `[R]` for the next shepherd pass.
- 2026-08-17 04:33 UTC - T37 merged as PR #36 (squash `627d102330b3987a4da5091d1431f14b82b06565`). CI passed on final head `00fa507cd919956edb93ddc3d612b45197abea7d`; Codex reviewed that exact head, and no Claude review workflow is configured. Its Completed-placement finding was fixed and resolved in `00fa507`; its non-array-open finding was verifier-classified invalid because `renderQuestions` already guards `!Array.isArray(open)` before iteration, with the disposition retained in its unresolved [thread](https://github.com/MrZoller/factory-ui/pull/36#discussion_r3793814977). No parked minors were added. A late-verdict marker was recorded at merge with two counted bot reviews/comments.
- 2026-08-17 04:47 UTC - T38 implemented and opened as PR #37. Added a keyboard-reachable Operators lane above the `/how` pipeline with Human, Claude Code operator session, and `bin/factory` nodes, operator-assisted and dashed direct-engine paths, an identical-operation caption, responsive styling, and text-only rendering coverage. Reclassified T38 from trivial to standard after the implementation exceeded the trivial line budget; approved gates remained satisfied. Verification: `bun test` (780 pass, 0 fail, 1903 assertions), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). The local panel confirmed one minor duplicate per-machine ARIA title id; it was fixed with a direct static label, and both re-review lenses plus the verifier returned CLEAR. One shepherd pass found the test check green, no labels, holds, threads, comments, findings, or Claude review workflow, and a mergeable exact head `cb276fa0aeaf1d6213fdfeea5a079a21693e7ffb`; Cursor and Cloudflare suites remain queued and Codex is configured with review still in flight (👀), so T38 remains `[R]` for the next shepherd pass.
- 2026-08-17 04:50 UTC - T38 merged as PR #37 (squash `ab39a40bfb24325a45d525aa3812a09a941a66ef`). CI passed on final head `cb276fa0aeaf1d6213fdfeea5a079a21693e7ffb`; Codex reviewed that exact head, and no Claude review workflow is configured. Its [operator-lane focus](https://github.com/MrZoller/factory-ui/pull/37#discussion_r3793869647) finding was independently verifier-classified minor, dispositioned in the thread, and parked in new rolling batch T39 without a minors-only push. No refutations or late bot verdicts required records.
- 2026-08-17 05:24 UTC - T34 implemented and opened as PR #38. Added a bounded `.factory/metrics.jsonl` reader with latest-valid-event folding, snapshot/API exposure, task-level internal and external review chips, missing-metrics markers, repository aggregates by task size, and highlighted mechanical round cross-checks; peer metrics are validated and hostile strings remain text-only. Verification: `bun test` (787 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff main...HEAD --check` (passed). Both local review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass found CI green, no labels, holds, threads, findings, or Claude workflow, and a clean mergeable exact head `a2ff3d1ca85297afd97ba7fe3a0ccf4deba730cc`; Codex is configured and its PR-body 👀 shows review is still in flight, so T34 remains `[R]` until an attributable verdict permits merge.
- 2026-08-17 05:29 UTC - T34 merged as PR #38 (squash `547c35ee851580c218cf1f37fa0516d9e0bc4072`). CI passed on head `a2ff3d1ca85297afd97ba7fe3a0ccf4deba730cc`; Codex supplied an attributable clean +1 verdict on the single-head PR; no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations.
- 2026-08-17 05:31 UTC - No task was runnable after reconciliation and sync: every planned task is complete except T39, the rolling parked-review-minors batch, which remains `[!]` with only one accumulated minor and no human question to reconcile. Set the factory phase to `idle`; no implementation or verification commands were required this cycle.
- 2026-08-17 05:44 UTC - T40 implemented and opened as PR #39. Grouped routing overrides into aligned provider/model lanes with ordered bold agent names and optional step caps, retained bounded responsive overflow and text-only hostile-string handling, and added focused regression coverage. Reclassified T40 from trivial to standard after implementation and tests exceeded the trivial line budget; approved standard gates remained satisfied. Verification: `bun test` (788 pass, 0 fail, 1947 assertions), `bun run lint` (Prettier and strict TypeScript passed), and `git diff main...HEAD --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned trivial).
- 2026-08-17 05:47 UTC - T40 PR #39 shepherd pass found CI green on exact head `9d27b181f986af8d004e01ae4773d9c739725c45`, no labels, hold, review threads, comments, findings, Claude review workflow, or merge conflict. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable verdict exists yet, so T40 remains `[R]` for the next shepherd pass. CI count: 1 run / 0 reruns; no external review round has completed.
- 2026-08-17 05:51 UTC - T40 PR #39 final review tally for head `9d27b181f986af8d004e01ae4773d9c739725c45`: Codex 1 round, 0 blocking / 1 minor / 1 refuted, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. The verified compound-key collision is parked with a disposition reply at https://github.com/MrZoller/factory-ui/pull/39#discussion_r3794065364; the mobile-overflow claim is refuted with evidence at https://github.com/MrZoller/factory-ui/pull/39#discussion_r3794065368. CI is green, the branch is cleanly mergeable, and there is no hold label.
- 2026-08-17 05:52 UTC - T40 merged as PR #39 (squash `e1c9622a380ae982dc7dbaad943d0c3062667cfa`). CI passed on final head `9d27b181f986af8d004e01ae4773d9c739725c45`; Codex reviewed that exact head. Its compound provider/model key collision was verifier-classified minor, dispositioned at https://github.com/MrZoller/factory-ui/pull/39#discussion_r3794065364, and parked in rolling batch T39 without a minors-only push. Its mobile-overflow finding was verifier-classified invalid and refuted with evidence at https://github.com/MrZoller/factory-ui/pull/39#discussion_r3794065368; the thread remains unresolved as the durable human record. No Claude review workflow is configured. T39 has two parked minors and remains intentionally blocked, so the factory is idle.
- 2026-08-17 18:05 UTC - T39 implemented and opened as PR #40. Preserved keyboard focus on the `/how` Operators lane across peer-fan-out rerenders and replaced ambiguous slash-joined routing keys with collision-free serialized provider/model tuples, with regressions for both parked review minors. Reclassified T39 from trivial to standard after implementation and test coverage exceeded the trivial line budget; approved standard gates remained satisfied. The synced plan initially failed Prettier, so its affected acceptance prose was normalized without changing separator or legacy-heading semantics. Verification: `bun test` (789 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 0 blocking / 1 minor / 0 invalid; 1 fixed; shipped standard (planned trivial). The confirmed minor was plan prose drift from formatter normalization; the fix re-panel and verifier returned CLEAR.
- 2026-08-17 18:07 UTC - T39 PR #40 shepherd pass found CI green on exact head `651306f90ab9ff4c1c5dbf37b52d237f247fb5b7`, no labels, hold, review threads, comments, findings, requested reviewers, or Claude review workflow. CI count: 1 run / 0 reruns. Codex is configured and its PR-body 👀 shows exact-head review is still in flight; no attributable verdict exists yet, so T39 remains `[R]` for the next shepherd pass.
- 2026-08-17 18:38 UTC - T39 PR #40 final review tally for head `651306f90ab9ff4c1c5dbf37b52d237f247fb5b7`: Codex 1 round, 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; CI 1 run / 0 reruns. Codex's single-head PR-body +1 is attributable to this head; no Claude review workflow is configured.
- 2026-08-17 18:59 UTC - T39 merged as PR #40 (squash `14367f8`). CI passed on head `651306f90ab9ff4c1c5dbf37b52d237f247fb5b7`; Codex supplied an attributable clean +1 verdict on this single-head PR, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations.

- 2026-08-17 19:27 UTC - T41 implemented and opened as PR #41. Added bounded legacy heading-form worklog parsing, safe headline/body rendering, and repeat-warning collapse with focused parser and browser regressions. Verification: `bun test` (795 pass, 0 fail, 1983 assertions), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 0 blocking / 2 minor / 0 invalid; 2 fixed; shipped standard (planned standard). The first confirmed minor added explicit `###` non-boundary coverage; the fix re-panel found and corrected ambiguous formatter-normalized separator prose before shipping.

- 2026-08-17 19:31 UTC - T41 PR #41 shepherd pass found CI green on exact head `44188789e35f459ccffe6e0441ae19d7fbefbc51`, no labels, hold, review threads, comments, findings, requested reviewers, Claude review workflow, or merge conflict. CI count: 1 run / 0 reruns. Codex is configured and briefly showed an in-flight eyes reaction, but no attributable exact-head verdict has landed within the initial review window; no nudge is permitted yet, so T41 remains `[R]` for the next shepherd pass.

- 2026-08-17 20:07 UTC - T41 PR #41 final review tally for head `44188789e35f459ccffe6e0441ae19d7fbefbc51`: Codex 1 round, 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; CI 1 run / 0 reruns. Codex posted a clean exact-head review comment; no Claude review workflow is configured. CI is green, there are no review threads or hold label, and the branch is cleanly mergeable.

- 2026-08-17 20:08 UTC - T41 merged as PR #41 (squash `1da4ad4`). CI passed on head `44188789e35f459ccffe6e0441ae19d7fbefbc51`; Codex posted a clean exact-head review comment, and no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations.

- 2026-08-17 20:09 UTC - No task was runnable after reconciliation and sync: every planned and ad-hoc task is complete, with no open questions or mid-flight task to resume. Set the factory phase to `idle`; no implementation or verification commands were required this cycle.
- 2026-08-21 02:21 UTC - Approved plan after verifying Approach, Tasks, and Risks are present and non-empty, spec approval is current, and T46 is runnable; set phase to `build`.
- 2026-08-21 02:42 UTC - T46 implemented and opened as PR #49. Scoped arbitrary wrapping to untrusted long-text surfaces, gave fleet/repository/review/task tables explicit sizing and internal overflow, kept identifiers/pills/numeric cells/review labels intact, and added nonbreaking driver timing spans plus CSS/DOM regressions. Verification: `bun test` (797 pass, 0 fail, 2002 assertions), `bun run lint` (Prettier and strict TypeScript passed), `git diff --check` (passed), and headless Chrome visual inspection at 390/768/1100/1440 px (passed). Panel: 1 round; findings 0 blocking / 0 minor / 1 invalid; 0 fixed; shipped standard (planned standard). The verifier rejected the sole finding because `overflow-wrap` is inherited by question-entry children.
- 2026-08-21 02:44 UTC - T46 PR #49 shepherd pass found CI green on exact head `58003b389001e426e8aac54b18cd262ebe67e413` with 1 run / 0 reruns, no labels, hold, review threads, comments, findings, Claude workflow, merge conflict, or branch staleness. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable verdict exists yet, so T46 remains `[R]` for the next shepherd pass.
- 2026-08-21 02:49 UTC - T46 PR #49: verifier independently classified both Codex narrow-layout findings as blocking. Pushed fix for Codex findings: `1cafc35`, restoring the task-table 42rem minimum width and adding contained horizontal overflow for intrinsically sized warning rows; replied to and resolved both fixed threads. Verification: `bun test` (797 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). CI and Codex must review the new head before merge.
- 2026-08-21 03:16 UTC - T46 PR #49 final review tally for head `fdaef64b682bedb73577054b677f1dcc52532160`: Codex 3 rounds, 2 blocking / 2 minor / 0 refuted findings, 1 fix push; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 3 runs / 0 reruns. The two current-head findings are verifier-classified minor and dispositioned for rolling batch T42 at https://github.com/MrZoller/factory-ui/pull/49#discussion_r3827008776 and https://github.com/MrZoller/factory-ui/pull/49#discussion_r3827008849. CI is green, there is no hold label, and the branch is cleanly mergeable.
- 2026-08-21 03:17 UTC - T46 merged as PR #49 (squash `877258aebc9f5e4b2371617370c976fd8971774b`). CI passed on final head `fdaef64b682bedb73577054b677f1dcc52532160`; Codex reviewed that head across three rounds. Its two earlier blocking findings were fixed in `1cafc35`; its two final findings were verifier-classified minor, dispositioned, and parked in rolling batch T42. No Claude review workflow is configured and no refutations or late verdicts required records.
- 2026-08-21 03:39 UTC - T47 implemented and opened as PR #50. Reorganized repository cards into gap-free status, task, worklog/warnings, review, and completed rows; collapsed empty task groups into one full-width strip; moved availability into Current; and preserved responsive collapse, disclosure state, and text-only hostile-string handling. Verification: `bun test` (799 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard). Both review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-21 03:42 UTC - T47 PR #50 shepherd pass found CI green (1 run / 0 reruns), no labels, hold, review threads, findings, or Claude review workflow, and a clean mergeable head `08a30ca081ead8c7c27588a7727d3512c3e8c1ec`. Codex is configured and its 👀 reaction shows review is still in flight; no attributable verdict exists yet, so T47 remains `[R]` for a later shepherd pass. No nudge was warranted.
- 2026-08-21 03:43 UTC - T47 review tally for PR #50 head `08a30ca081ead8c7c27588a7727d3512c3e8c1ec`: Codex 1 round; 0 blocking / 0 minor / 0 refuted findings; 0 fix pushes. Claude is not configured (no workflow). CI: 1 run / 0 reruns. The attributable single-head Codex +1 verdict is clean; no review threads or findings require verifier classification.
- 2026-08-21 03:44 UTC - T47 merged as PR #50 (squash `7a0e2b22fbfc22ced2ea0be03397876c7b6c16f6`). CI passed on head `08a30ca081ead8c7c27588a7727d3512c3e8c1ec`; Codex supplied an attributable clean +1 verdict on this single-head PR; no Claude review workflow is configured. There were no review threads, findings, holds, parked minors, or refutations. Post-merge metrics record Codex 1 clean round and CI 1 run / 0 reruns.
- 2026-08-21 04:19 UTC - T43 implemented and opened as PR #51. Consolidated chip, button, tab, panel-title, empty-state, typography, semantic color, numeric-cell, and dark-mode primitives; gave both pages a compact responsive header; and normalized summary placeholders while preserving Unknown and Unavailable reader states and text-only rendering. Verification: `bun test` (801 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff main...HEAD --check` (passed). Panel: 2 rounds; findings 1 blocking / 2 minor / 0 invalid; 3 fixed; shipped standard (planned standard). The first panel confirmed mixed-state hold rendering and two CSS-test gaps; all were fixed, and the re-panel plus verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-21 04:20 UTC - T43 PR #51 shepherd pass found CI green (1 run / 0 reruns), no labels, hold, reviews, comments, review threads, Claude workflow, merge conflict, or branch staleness on exact head `38c21614e3988b9ec33c10a402768004af8dfb4e`. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable verdict exists yet, so T43 remains `[R]` for a later shepherd pass. No nudge was warranted and no files were changed.
- 2026-08-21 04:23 UTC - T43 PR #51 review tally for head `38c21614e3988b9ec33c10a402768004af8dfb4e`: Codex 1 round; verifier-classified findings 0 blocking / 1 minor / 0 refuted; 0 fix pushes. Claude is not configured (no `claude-code-review.yml` workflow). CI 1 run / 0 reruns, green. The sole Codex thread was dispositioned and parked as a minor; no blocking findings remain.
- 2026-08-21 04:28 UTC - T43 merged as PR #51 (squash `38b8a0e25e6e1d63ae6fc373367431ea221bb12a`) after green CI on head `38c21614e3988b9ec33c10a402768004af8dfb4e`. Codex delivered one review round with one verifier-classified minor, parked in rolling batch T42; no fix push, refutation, or Claude workflow verdict was required.
- 2026-08-21 04:53 UTC - T44 implemented and opened as PR #52. Decluttered review, task, worklog, routing, badge, and summary surfaces while preserving disclosure state, metrics semantics, text-only rendering, and strict GitHub URL validation. Verification: `bun test` (800 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard). Both review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-21 04:55 UTC - T44 PR #52 shepherd pass found CI green (1 run / 0 reruns), no labels, hold, comments, reviews, review threads, findings, Claude review workflow, merge conflict, or branch staleness on exact head `c380718d0e15ae770785e413631097c58b2f42ec`. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable verdict exists yet, so T44 remains `[R]` for a later shepherd pass. No nudge was warranted and no files were changed.
- 2026-08-21 04:59 UTC - T44 PR #52 review tally for head `c380718d0e15ae770785e413631097c58b2f42ec`: Codex 1 round; verifier-classified findings 0 blocking / 2 minor / 0 refuted; 0 fix pushes. Claude is not configured (no `claude-code-review.yml` workflow). CI 1 run / 0 reruns, green. Both Codex threads were dispositioned and parked as minors; no blocking findings remain.
- 2026-08-21 05:00 UTC - T44 merged as PR #52 (squash `0b4ab0dc62fdc5d96e2e6474c8b94b7c19d6af1a`) after green CI and Codex's exact-head review. Its two verifier-classified minors were recorded in rolling batch T42; the batch reached five items and is now runnable. Post-merge metrics record Codex 1 round, 0 blocking / 2 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-21 05:17 UTC - T42 implemented and opened as PR #53. Applied five parked UI review minors: wide task tables fill their panel while preserving internal scrolling, reviewer labels remain intact, `CANNOT_VERIFY` chips retain their muted tone, unverified review cross-checks are distinct from matches, and Unknown question badges keep their distinct tooltip. Verification: `bun test` (801 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped trivial (planned trivial). Both review lenses returned CLEAN and the adversarial verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-21 05:18 UTC - T42 PR #53 shepherd pass found CI green (1 run / 0 reruns), no labels, hold, comments, reviews, review threads, findings, Claude review workflow, merge conflict, or branch staleness on exact head `ba44d4fcec108a546641957c2a90f085566e3d8c`. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable verdict exists yet, so T42 remains `[R]` for a later shepherd pass. No nudge was warranted and no files were changed.
- 2026-08-21 05:23 UTC - T42 PR #53 final review tally for head `ba44d4fcec108a546641957c2a90f085566e3d8c`: Codex 1 round, 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. Codex's single-head attributable PR-body 👍 is its clean exact-head verdict. CI is green, no review threads or hold label remain, and the branch is cleanly mergeable.
- 2026-08-21 05:24 UTC - T42 merged as PR #53 (squash `1318783e4c77b46cae100fa9d885031aa34b9c81`) after green CI on final head `ba44d4fcec108a546641957c2a90f085566e3d8c` and Codex's attributable clean verdict. No review findings, threads, holds, refutations, or Claude workflow verdict required action. Post-merge metrics record Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-21 05:41 UTC - T45 implemented and opened as PR #54. Made the `/how` pipeline and operator lane responsive without diagram overflow, removed fixed stage heights, moved model limits and prices into closed disclosures, unified approval and hold gates, and retained full route identifiers in safe title text while preserving focus, routing, peer fan-out, and text-only rendering. Verification: `bun test` (804 pass, 0 fail, 2133 assertions), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 0 blocking / 1 minor / 0 invalid; 1 fixed; shipped standard (planned standard). The confirmed test-adequacy minor was fixed by asserting every model field remains inside exactly one per-role disclosure; the re-panel and verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-21 05:43 UTC - T45 PR #54 shepherd pass found CI green on exact head `c9680a533b286fa3074cd72c2a1734c69ffef1b8` with 1 run / 0 reruns, no labels, hold, comments, reviews, review threads, findings, Claude review workflow, merge conflict, or branch staleness. Codex is configured and its PR-body 👀 shows review is still in flight; no attributable verdict exists yet, so T45 remains `[R]` for a later shepherd pass. No nudge was warranted and no files were changed.
- 2026-08-21 05:48 UTC - T45 PR #54 review tally for head `c9680a533b286fa3074cd72c2a1734c69ffef1b8`: Codex 1 round; verifier-classified findings 0 blocking / 2 minor / 0 refuted; 0 fix pushes. Claude is not configured (the `claude-code-review.yml` workflow is absent). CI 1 run / 0 reruns, green. Both Codex threads received minor dispositions and are parked for the next rolling review-minors batch; no blocking findings remain.
- 2026-08-21 05:49 UTC - T45 merged as PR #54 (squash `946a8519b9203f19229040d2ffe3fa416e708e14`) after green CI on head `c9680a533b286fa3074cd72c2a1734c69ffef1b8` and Codex's exact-head review. Its two verifier-classified minors were recorded in rolling batch T48; no fix push, refutation, or Claude workflow verdict was required. Post-merge metrics record Codex 1 round, 0 blocking / 2 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-21 05:50 UTC - No runnable task remained after sync. Reconciled T45's stale `[R]` plan marker to `[x]` from its recorded PR #54 merge and completion bookkeeping. Rolling parked-minors batch T48 remains intentionally `[!]` with two items, below the five-item runnable threshold established for the preceding batch; set the factory phase to `idle`. Verification: `gh pr view 54 --json number,state,mergedAt,headRefName,baseRefName,url` confirmed PR #54 is merged; no product tests were needed for bookkeeping-only changes.
- 2026-08-29 19:39 UTC - Approved plan after verifying all required sections are present and non-empty, the spec is approved, and runnable tasks T51 and T52 remain; set the factory phase to `build` so the line selects T51 next.
- 2026-08-29 19:40 UTC - Correction to the preceding approval entry: the next run first reconciles answered Q1-Q3, keeping T49 blocked on its external contract and releasing T50, so T50 precedes currently runnable T51 and T52 in task selection.
- 2026-08-29 19:42 UTC - Reconciled answered Q1-Q3. T49 remains blocked until MrZoller/opencode-factory#83 supplies the engine-owned intake contract; its accepted authorization policy now requires a minimal shared secret plus a recorded verified actor identity. Released T50 with the approved omission of fabricated question age and deterministic repository/question-id ordering. Marked all three applied answers consumed; phase remains `build` because the approved plan has runnable work. Verification: inspected the answered question text and updated acceptance criteria; no product tests were needed for bookkeeping-only reconciliation.
- 2026-08-29 20:13 UTC - T50 implemented and opened as PR #62. Added a bounded fleet-wide question queue with structured current-grammar parsing, exact blocked-task and safe GitHub links, local/peer aggregation, deep links, deterministic repository/question-id ordering, a top-256 display cap with total/truncation signal, mixed-peer isolation, and text-only rendering; omitted fabricated question age as approved. Verification: `bun test` (812 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 1 blocking / 1 minor / 0 invalid; 2 fixed; shipped standard (planned standard). The initial panel confirmed an unbounded aggregate rendering path and a malformed-peer coverage gap; both were fixed, and the re-panel plus verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-29 20:16 UTC - T50 PR #62 shepherd pass found CI green on exact head `903ad37e1931968145ad036baae8171457a79a87` with 1 run / 0 reruns, no hold, labels, findings, review threads, branch staleness, or merge conflict. Claude is not configured. Codex is configured and its PR-body 👀 shows review is still in flight; no completed attributable verdict exists yet, so T50 remains `[R]` for a later shepherd pass. The PR is inside the initial review window, so no nudge was warranted and no files were changed.
- 2026-08-29 20:24 UTC - T50 PR #62 final review tally for head `903ad37e1931968145ad036baae8171457a79a87` (gatheredAt `2026-08-29T20:24:15Z`): Codex 1 round, verifier-classified 0 blocking / 2 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. The two Codex threads were dispositioned and parked for the rolling review-minors batch at https://github.com/MrZoller/factory-ui/pull/62#discussion_r3887593907 and https://github.com/MrZoller/factory-ui/pull/62#discussion_r3887593937. A cursor-bound final gather found checks unchanged and green, no hold label, stable review/comment/reaction/thread surfaces, and both parked threads addressed; the branch is cleanly mergeable.
- 2026-08-29 20:25 UTC - T50 merged as PR #62 (squash `ebf8259`). CI passed on head `903ad37e1931968145ad036baae8171457a79a87`; Codex delivered one exact-head review round with two verifier-classified minor findings, dispositioned and parked in rolling batch T48 without a minors-only push. No Claude review workflow is configured, and no blocking findings, refutations, or holds remain. Post-merge metrics record Codex 1 round, 0 blocking / 2 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-29 20:35 UTC - T51 implemented and opened as PR #65. Updated bounded log-name parsing to accept the engine's exact kind-specific grammar, including shepherd PID/pass suffixes and deterministic timestamp/PID/pass ordering; added real-name, malformed-arity, calendar, selection, timing, `asOf`, and API warning regressions while preserving existing scan and identity protections. Verification: `bun test` (818 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard). Both review lenses and the adversarial verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-29 20:37 UTC - T51 PR #65 shepherd pass found CI green on exact head `95b1506165d547abc857f224c870f026464049ba` with 1 run / 0 reruns, no review threads, findings, labels, hold, branch staleness, or merge conflict. Claude is not configured. Codex is configured and its PR-body 👀 shows review is still in flight; no completed attributable verdict exists yet, so T51 remains `[R]` for a later shepherd pass. The review began at `2026-08-29T20:35:43Z`, inside the initial review window, so no nudge was warranted and no files were changed.
- 2026-08-29 20:39 UTC - T51 PR #65 final review tally for head `95b1506165d547abc857f224c870f026464049ba` (gatheredAt `2026-08-29T20:39:30Z`): Codex 1 round, verifier-classified 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. Codex's attributable single-head PR-body +1 was clean. A cursor-bound final gather found the exact-head check unchanged and green, no review threads or hold label, and stable review/comment/reaction/thread surfaces; the verified local task branch matches the remote head and is cleanly mergeable.
- 2026-08-29 20:40 UTC - T51 merged as PR #65 (squash `4d9a59ac5cc342978f91c2afa07f3a2d60e0d8e8`). CI passed on head `95b1506165d547abc857f224c870f026464049ba`; Codex delivered one clean attributable single-head +1 review round. No Claude workflow is configured, and no findings, review threads, holds, parked minors, or refutations remain. Post-merge metrics record Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-29 21:02 UTC - T52 implemented and opened as PR #66. Raised the fixed log-directory scan bound to 4,096 while retaining constant-memory deterministic newest-per-kind selection, added a dedicated over-bound diagnostic that suppresses untrusted liveness probes, and documented the UI warning and operator remedy. Added reader, liveness, snapshot, and API regressions above the former 256-entry limit and at the new boundary. Verification: `bun test` (824 pass, 0 fail, 2210 assertions), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 0 blocking / 1 minor / 0 invalid; 1 fixed; shipped standard (planned standard). The confirmed test-adequacy minor was fixed with positive snapshot and API overflow-propagation assertions; the re-panel and verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-29 21:04 UTC - T52 PR #66 shepherd pass on head `0877096aa246977310e277f0c3ef88ec2b302d92` found CI run 33274996647 attempt 1 green (1 run / 0 reruns), no labels, hold, findings, review threads, branch staleness, or merge conflict. Claude is not configured. Codex is configured and its review began at `2026-08-29T21:02:03.672393Z`; only the in-flight PR-body eyes signal was present at `2026-08-29T21:03:40Z`, so T52 remains `[R]` for a later shepherd pass. No nudge was warranted inside the initial review window and no files were changed.
- 2026-08-29 21:06 UTC - T52 PR #66 final review tally for head `0877096aa246977310e277f0c3ef88ec2b302d92`: gatheredAt `2026-08-29T21:06:27Z`; Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes; Claude not configured; CI 1 run / 0 reruns. The cursor-bound terminal sweep was stable (one Codex summary comment and one attributable PR-body +1, no reviews, comment reactions, or threads); CI remained green and the branch mergeable.
- 2026-08-29 21:07 UTC - T52 merged as PR #66 (squash `b95c953552b9c25d5f87e26dd8abf577f396faac`). CI passed on head `0877096aa246977310e277f0c3ef88ec2b302d92`; Codex delivered one clean attributable review round; Claude is not configured. There were no findings, review threads, holds, parked minors, or refutations. Post-merge metrics record Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-29 21:09 UTC - No runnable task remained after reconcile and sync. T49 stays blocked pending the engine-owned answer-intake contract in MrZoller/opencode-factory#83. Rolling parked-minors batch T48 stays intentionally `[!]` with four items, below the established five-item runnable threshold. Set the factory phase to `idle`; no product verification was needed for bookkeeping-only changes.
- 2026-08-30 14:03 UTC - T49 implemented the authenticated answer-intake API, durable lifecycle queue, direct owning-peer routing, exact engine question grammar, private restart-safe idempotency records, documentation, and regression coverage, then was parked on `factory/t49-answer-delivery` after the allowed re-panel still BLOCKED. Reclassified T49 from standard to major because the completed change introduces the repository's first authenticated write boundary and exceeds 3,000 changed lines. Verification: `bun test` (855 pass, 0 fail, 2343 assertions), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Panel: 2 rounds; verifier classifications total 3 blocking / 1 minor / 1 invalid; 3 fixed. The remaining blocking finding is an unavoidable ambiguity in the current engine helper: it may publish a record and fail before returning the UUID, so retry safety requires either retaining a fail-closed reservation for manual inspection or extending the engine contract with a client idempotency key. Filed Q4 for that human design choice; no runnable task remains because T48 is intentionally below its five-minor threshold, so phase is `idle`.
- 2026-08-30 16:46 UTC - Reconciled the answer to Q4 for T49: ambiguous `factory-answers` helper failures retain the durable idempotency reservation and require operator verification before manual cleanup, preserving fail-closed at-most-once behavior. Marked Q4 consumed and restored T49 with parked branch `factory/t49-answer-delivery` for completion. Verification: inspected the attributed answer and parked branch; no product tests were needed for bookkeeping-only reconciliation.
- 2026-08-30 17:14 UTC - T49 shipped as held major PR #71 after applying Q4's fail-closed policy: ambiguous helper failures retain the server reservation, the browser durably preserves the same key and answer payload before sending, the shared secret is never persisted, and manual cleanup requires operator verification. Added reload, storage-failure, capacity, disappeared-question, and hostile-record regressions. Verification: `bun test` (859 pass, 0 fail, 2389 expectations), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Panel: 4 rounds; findings 4 blocking / 4 minor / 1 invalid; 8 fixed; shipped major (planned standard). PR is held because T49 introduces the dashboard's first authenticated write boundary.
- 2026-08-30 17:31 UTC - T49 PR #71 verifier classified Codex's unbounded answer submit/outcome requests as blocking; added five-second abort timeouts that clear the sending/tracking state, with stalled submit and outcome-poll regressions. Verification: `bun run lint` (pass); `bun test src/public/app.test.ts` (120 pass, 0 fail); `bun test` (861 pass, 0 fail). Pushed fix for Codex findings: `3209a4a`.
- 2026-08-30 17:41 UTC - T49 PR #71 final review tally for head `3209a4aba694be811614d760f8862611344125fb` (gatheredAt `2026-08-30T17:41:37Z`): Codex 2 rounds, verifier-classified 1 blocking / 2 minor / 0 refuted findings, 1 fix push; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 2 runs / 0 reruns. The crash-left temporary-idempotency-file finding was verifier-classified minor and dispositioned at https://github.com/MrZoller/factory-ui/pull/71#discussion_r3890070261; it joins the rolling parked-minors batch, with no minors-only push. A cursor-bound final gather found the exact-head check unchanged and green, no hold label, and stable reviews (5, newest 5061481183), issue comments (3, newest 5470205706), PR-body reactions (0), and threads (3 / 6 comments, newest 3890070261); all threads are fixed or parked-with-reply. The verified local task branch matches the remote head and is cleanly mergeable, but T49 remains held because it is major and awaits operator merge authority.
- 2026-08-30 17:45 UTC - T49 merged as PR #71 (squash `28b9acfd40aada74c2cb567ab3cf260d85a80803`) under human authority for the held major. The committed final tally covers Codex's two rounds: one blocking finding fixed in `3209a4a` and two parked minors; Claude is not configured; CI had 2 runs / 0 reruns. Parked the documented read-only-boundary drift, crash-left temporary idempotency files, and uncertain-state README disclosure in rolling batch T48; it now exceeds the five-item batching threshold and is runnable. Post-merge bookkeeping cleared the task state and hold.
- 2026-08-30 17:55 UTC - T48 reclassified from trivial to standard because resolving seven parked review minors plus focused regressions changed more than 350 lines across the dashboard, snapshot enrichment, idempotency store, and documentation. The approved spec and plan gates satisfy the standard-task gate; scope remains the listed parked findings.
- 2026-08-30 18:11 UTC - T48 implemented and opened as PR #72. Resolved seven parked review minors: preserved `/how` model-disclosure state and focus, wrapped long model names, suppressed ambiguous blocked-task enrichment and duplicate question links, documented the approved answer-intake exception and uncertain-state retention, and reclaimed crash-left idempotency files with serialized concurrent capacity checks. Verification: `bun test` (867 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned trivial). Both review lenses and the adversarial verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-30 18:20 UTC - T48 PR #72 final review tally for head `b7606e965867d55b5b9f08e3f8ba8c988927db95` (gatheredAt `2026-08-30T18:20:18Z`): Codex 1 round, verifier-classified 0 blocking / 0 minor / 1 refuted finding, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. The temporary-file interprocess finding was refuted with a gate-complete packet at https://github.com/MrZoller/factory-ui/pull/72#discussion_r3890159773 and its thread remains unresolved by convention. The cursor-bound terminal gather was stable: exact-head CI stayed green; reviews 2 (newest 5061562418), issue comments 1 (newest 5470402221), PR-body reactions 0, and threads 1 / 2 comments (newest 3890159773); there is no hold label and the verified local task branch matches the remote head.
- 2026-08-30 18:21 UTC - T48 merged as PR #72 (squash `97f7d1a22b3be0220aa027cd59750a3257a013bf`). CI passed on final head `b7606e965867d55b5b9f08e3f8ba8c988927db95`; Codex completed one exact-head review round. Its temporary-file interprocess finding was verifier-classified invalid and refuted with the gate-complete thread reply; the unresolved refutation remains the durable human record. Claude is not configured. Post-merge metrics record Codex 1 round, 0 blocking / 0 minor / 1 refuted finding, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-31 00:18 UTC - No runnable task remained after answer reconciliation and sync: every plan task is complete, no task is mid-flight, and no open or answered-unconsumed question remains. Set the factory phase to `idle`. Verification: inspected `.factory/plan.md`, `.factory/state.json`, `.factory/questions.md`, and `.factory/metrics.jsonl`; no product tests were needed for bookkeeping-only changes.
- 2026-08-31 02:17 UTC - Synced the `factory`-labelled GitHub backlog into T53-T61. Imported nine newly open issues without changing existing task wording, size, numbering, or completion records; directly verified MrZoller/opencode-factory#92 is closed as completed, so T54's external prerequisite is satisfied and needs neither dependency metadata nor a blocker question. Reset plan approval and moved the phase to `plan` so no imported task runs before `/approve plan`. Verification: `gh issue list --state open --limit 1000 --label "factory" --json number,title,body,labels` returned 9 issues; `gh issue view 92 --repo MrZoller/opencode-factory --json state,stateReason,title` returned `CLOSED` / `COMPLETED`; no product tests were needed for bookkeeping-only changes.
- 2026-08-31 10:16 UTC - Synced the `factory`-labelled GitHub backlog and appended T62 for newly open issue #73. Directly verified every non-terminal issue-backed task T53-T61 remains open; no closures or reopens changed existing records. Reset plan approval and kept the phase at `plan` so the appended task cannot run before `/approve plan`. Verification: `gh issue list --state open --limit 1000 --label "factory" --json number,title,body,labels` returned 10 issues; direct `gh issue view N --json state,stateReason` queries returned `OPEN` for #70, #69, #68, #67, #64, #63, #59, #58, and #55; no product tests were needed for bookkeeping-only changes.
- 2026-08-31 10:19 UTC - Approved plan for backlog tasks T53-T62 (operator, Chris's explicit gate approval in the operator session). The `/approve plan` command failed twice at its new settle step: it invokes `bin/factory-answers settle` repo-relative, which exists only in the engine repo, and the session sandbox correctly refuses the machine-global fallback read — reported upstream to the opencode-factory line. Settle-equivalent verified by operator: `factory-answers pending-count` = 0 in this clone, so the transaction had nothing to settle. Set `plan_approved: true`, phase `build`.
- 2026-08-31 10:41 UTC - T53 implemented and opened as PR #74. Accepted legacy and bounded RFC3339 UTC `filed-at` question headings, retained otherwise valid questions while warning on malformed or misplaced timestamp grammar, and rendered authoritative question age in repository panels and the fleet queue while omitting age for legacy entries and keeping hostile text inert. Verification: `bun test` (869 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 1 blocking / 0 minor / 0 invalid; 1 fixed; shipped standard (planned standard). The confirmed closed-entry marker-validation gap was fixed with coverage for answered questions; the re-panel and verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-31 10:47 UTC - T53 PR #74 final review tally for head `5b1e6a970d9e30ed0e6b004f6c9e3dd7ebe14187` (gatheredAt `2026-08-31T10:47:42Z`): Codex 1 round, verifier-classified 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. Codex's attributable clean PR-body +1 is valid on this single-head PR (no force-pushes and no commits after PR creation). A cursor-bound final gather was stable: exact-head CI remained green; reviews 0, issue comments 1 (newest 5477182300), PR-body reactions 1 (newest 479761713), comment reactions 0, and threads 0. There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 10:48 UTC - T53 merged as PR #74 (squash `141c31b223bf0e58a969f57af9af149af061042e`). CI passed on head `5b1e6a970d9e30ed0e6b004f6c9e3dd7ebe14187`; Codex delivered one clean attributable single-head +1 review round; Claude is not configured. There were no findings, review threads, holds, parked minors, or refutations. Post-merge metrics record Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-31 11:28 UTC - T54 implemented and opened as PR #75. Added a bounded machine/repository dependency graph with runnable, building, review, question-blocked, held, and done states; separated executable local dependencies from offline qualified cross-repository references; linked blocked tasks to the question queue and PR/issue identities through validated URLs; and isolated malformed, unavailable, or over-limit peer task data while preserving text-only responsive rendering. Optional `deps:` metadata now follows the completed protocol contract: omission means no prerequisites, while malformed declarations remain non-runnable and warned. Verification: `bun test` (876 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 1 blocking / 0 minor / 1 invalid; 1 fixed; shipped standard (planned standard). The confirmed unbounded peer-task traversal was capped before inspection and covered at the 257-task boundary; the omitted-deps finding was verifier-rejected against the then-current reader contract, but the implementation was independently aligned with the authoritative optional-deps protocol. Re-panel and verifier returned CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-31 11:38 UTC - T54 PR #75 final review tally for head `7e137c3fa35bf0b7fce249389697f21d7f4cb213` (gatheredAt `2026-08-31T11:38:04Z`): Codex 1 round, verifier-classified 0 blocking / 2 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. Both confirmed minor findings were dispositioned and parked without a minors-only push: unavailable peer graph grouping ([thread](https://github.com/MrZoller/factory-ui/pull/75#discussion_r3894041941)) and per-repository truncation rather than `No tasks` ([thread](https://github.com/MrZoller/factory-ui/pull/75#discussion_r3894041945)). The cursor-bound terminal gather was stable: exact-head CI remained green; reviews 3 (newest 5065965677), issue comments 1 (newest 5477650060), PR-body reactions 0, comment reactions 0, and threads 2 / 4 comments (newest 3894062840). There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 11:39 UTC - T54 merged as PR #75 (squash `5691dd86aa8c1d959d357512b7d4585edd602ab8`). CI passed on final head `7e137c3fa35bf0b7fce249389697f21d7f4cb213`; Codex completed one exact-head review round. Its two verifier-classified minor findings were dispositioned in their threads and parked in rolling batch T63 without a minors-only push. Claude is not configured. Post-merge metrics record Codex 1 round, 0 blocking / 2 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-31 14:56 UTC - T55 resumed after the prior session watchdog parked its implementation, completed the shared structured question renderer, and opened PR #76. Repository panels and the fleet queue now parse and render hard-wrapped labelled choices and legacy unlabelled slash-separated prose consistently; the proportional fallback removes duplicate heading/empty-answer scaffolding, joins hard wraps, distinguishes recognized fields, and keeps hostile source text inert. Verification: `bun test` (879 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Panel: 2 rounds; findings 1 blocking / 0 minor / 0 invalid; 1 fixed; shipped standard (planned standard). The confirmed prose-only answer-form crash was fixed with a guarded labelled-options path and regression coverage; the re-panel and verifier returned CLEAR.
- 2026-08-31 15:17 UTC - T55 PR #76 verifier classified Codex's hard-wrapped structured-option bound finding as blocking. Added the shared 8,192-character option limit; over-limit labelled or prose options now omit only structured fields, retain the original text for the safe fallback, and report `QUESTIONS_OPTION_TOO_LONG`. Verification: `bun test src/readers/questions.test.ts` (50 pass, 0 fail); `bun run lint` (Prettier and strict TypeScript passed); `git diff --check` (passed). Pushed fix for Codex findings: `819fc81`.
- 2026-08-31 15:22 UTC - T55 PR #76 CI failure diagnosed as a missing UI explanation for the new `QUESTIONS_OPTION_TOO_LONG` reader warning. Added the matching explanation. Verification: `bun test` (882 pass, 0 fail); `bun run lint` (Prettier and strict TypeScript passed). Pushed fix for CI failure: `9adfcee`.
- 2026-08-31 15:35 UTC - T55 PR #76 final review tally for head `9adfcee266fd3490ee1e0b9e365d15afed86118f` (gatheredAt `2026-08-31T15:35:37Z`): Codex 2 rounds, verifier-classified 1 blocking / 0 minor / 0 refuted findings, 1 fix push; Claude is not configured (the `claude-code-review.yml` workflow is absent). CI 2 runs / 0 reruns. Codex's current-head summary comment and +1 reaction record its clean verdict; the earlier blocking thread was fixed in `819fc81` and is resolved. A cursor-bound terminal gather was stable: exact-head CI remained green; reviews 2 (newest 5068112794), issue comments 1 (newest 5480149435), PR-body reactions 1 (newest 480189602), comment reactions 0, and threads 1 / 2 comments (newest PRRC_kwDOT5pyeM7oNLvE). There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 15:36 UTC - T55 merged as PR #76 (squash `01dd929bb0419389c6fcad6e4b3cfc8c653a0731`). CI passed on final head `9adfcee266fd3490ee1e0b9e365d15afed86118f`; Codex delivered two review rounds, with its one verifier-classified blocking finding fixed in `819fc81` and the thread resolved. Claude is not configured. Post-merge metrics record Codex 2 rounds, 1 blocking / 0 minor / 0 refuted, 1 fix push, and CI 2 runs / 0 reruns.
- 2026-08-31 15:54 UTC - T56 implemented and opened as PR #77. Structurally separated the Questions navigation label and count, kept fresh local snapshots in the calm Updated presentation while retaining peer-timeout, refresh-failure, and paused reasons, reserved Stale styling and coarse age for snapshots older than the configured refresh interval, and suppressed unchanged one-second status DOM writes. Verification: `bun test` (884 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard). Both review lenses and the adversarial verifier returned CLEAR.
- 2026-08-31 16:00 UTC - T56 PR #77 final review tally for head `fc39ef70e8d22d58ac7a4593f94e0fa1208cb97b` (gatheredAt `2026-08-31T16:00:59Z`): Codex 1 round, verifier-classified 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. Codex's exact-head completion summary and clean PR-body +1 are its verdict. A cursor-bound terminal gather was stable: exact-head CI remained green; reviews 0, issue comments 1 (newest 5480877128), PR-body reactions 1 (newest 480235460), comment reactions 0, and threads 0. There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 16:02 UTC - T56 merged as PR #77 (squash `301d75fa807efaede8e9c2d7efe136898013a87b`). CI passed on final head `fc39ef70e8d22d58ac7a4593f94e0fa1208cb97b`; Codex delivered one clean exact-head review round, and Claude is not configured. There were no findings, review threads, holds, parked minors, refutations, or late verdicts requiring records. Post-merge metrics record Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-31 16:19 UTC - T57 implemented and opened as PR #78. Replaced opaque `sub` labels with visible prepaid-subscription and token details, identified dollar totals as metered spend, renamed the repository bucket to Factory overhead with an operator-readable explanation, and distinguished prepaid, metered, unavailable, and absent overhead states without changing cost or notional calculations. Verification: `bun test src/public/app.test.ts` (132 pass, 0 fail), `bun test` (886 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard). Both review lenses and the adversarial verifier returned CLEAR.
- 2026-08-31 16:25 UTC - T57 review tally for PR #78, gatheredAt 2026-08-31T16:25:14Z: Codex rounds 1; findings 0 blocking / 0 minor / 0 refuted; fixPushes 0. Claude review workflow is not configured. CI runs 1; reruns 0. The cursor-bound final gather was stable on head `fb998303b423ba43b3056d30cf55fbbb27f8fc36`: CI `test` succeeded, Codex's exact-head completed review summary and clean PR-body +1 were present, and there were zero review threads, holds, or other findings.
- 2026-08-31 16:26 UTC - T57 merged as PR #78 (squash `41de5ad`). CI passed on final head `fb998303b423ba43b3056d30cf55fbbb27f8fc36`; Codex delivered one clean exact-head review round, and Claude is not configured. There were no findings, review threads, holds, parked minors, refutations, or late verdicts requiring records. Post-merge metrics record Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes, and CI 1 run / 0 reruns.
- 2026-08-31 16:38 UTC - T58 implemented and opened as PR #79. Replaced two-axis overscroll containment on task table and expanded-list scrollboxes with horizontal containment plus native vertical chaining, so wheel input can return to page scrolling at an inner boundary while wide tables remain internally scrollable; retained the 24rem expanded-list bound, keyboard focus, collapsed disclosure behavior, and body horizontal-overflow protection. Verification: `bun test src/public/app.test.ts` (132 pass, 0 fail), `bun test` (886 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard). Both review lenses and the adversarial verifier returned CLEAR.
- 2026-08-31 16:44 UTC - T58 PR #79 final review tally for head `48aeb0aea233fd14923ed392a4bba09628ddf6ef` (gatheredAt `2026-08-31T16:44:40Z`): Codex 1 round, verifier-classified 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent). CI 1 run / 0 reruns. Codex's exact-head completion summary and attributable clean PR-body +1 are its verdict. A cursor-bound terminal gather was stable: exact-head CI remained green; reviews 0, issue comments 1 (newest 5481405227), PR-body reactions 1 (newest 480313008), comment reactions 0, and threads 0. There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 16:45 UTC - T58 merged as PR #79 at `48aeb0aea233fd14923ed392a4bba09628ddf6ef`; marked complete and recorded external review metrics (Codex 1 clean round; CI 1 run / 0 reruns).
- 2026-08-31 17:43 UTC - T59 implemented and opened as PR #80. Added bounded immediate-child repository discovery from configured code roots, explicit-entry precedence, fixed shell-free GitHub-origin derivation, generic discovery warnings, and per-request refresh shared by fleet, repository, and answer routes. Preserved discovered root/child identities privately and fail closed around snapshot reads and answer results when paths change. Verification: `bun test` (902 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 1 blocking / 1 minor / 0 invalid; 2 fixed; shipped standard (planned standard). The confirmed post-discovery path-replacement leak and outcome-result recheck gap were fixed with persistent- and during-consumption swap regressions; final verdict CLEAR. One shepherd pass remains before merge eligibility is known.
- 2026-08-31 17:48 UTC - T59 PR #80 CI failure was a nondeterministic replacement-fixture assumption: deleting the candidate permitted inode reuse, so the identity-change assertion could pass or fail depending on filesystem allocation. Kept the original directory linked by renaming it before creating the replacement, making the identity check deterministic. Verification: `bun run lint` (pass); `bun test` first exposed an unrelated temporary-idempotency concurrency timeout, then one permitted environmental rerun passed (902 pass, 0 fail). Pushed fix for CI failure: `24af1f7`.
- 2026-08-31 17:57 UTC - T59 PR #80 verifier classified Codex's inode-reuse directory-identity finding as blocking. Retained a validated open handle to each accepted discovered directory, check that handle alongside path/device/inode identity before use, and close handles at the end of the API request so an unlinked original cannot have its inode reused as a replacement during the read. Verification: `bun run lint` (Prettier and strict TypeScript passed); `bun test` (902 pass, 0 fail); build: N/A (documented; `AGENTS.md` specifies no build). Pushed fix for Codex findings: `dfd2a8f`.
- 2026-08-31 18:06 UTC - T59 PR #80 CI failed because direct discovery callers leaked retained directory handles and Node treated garbage-collection close as an error. Closed tracked handles in test cleanup while retaining request-bound cleanup in the server. The verifier also confirmed the blocking fleet-discovery-warning finding; validated the optional bounded warning array and render its warning codes at the machine level. Verification: `bun run lint` (Prettier and strict TypeScript passed); `bun test` (902 pass, 0 fail); build: N/A (documented; `AGENTS.md` specifies no build). Pushed fix for Codex findings and CI failure: `7c819bd`.
- 2026-08-31 18:18 UTC - T59 merged as PR #80 (squash head `7c819bd93bf9b17fd84016629bf48f4aeed6ef75`). The stable final gather at `2026-08-31T18:17:26Z` found green exact-head CI, no hold, and every review thread addressed. Codex delivered 3 rounds: 2 verifier-classified blocking findings fixed in `dfd2a8f` and `7c819bd`, plus 2 confirmed minors parked in rolling batch T63; no findings were refuted. Claude is not configured. Recorded external metrics: Codex 3 rounds, 2 blocking / 2 minor / 0 refuted, 2 fix pushes; CI 4 runs / 0 reruns.
- 2026-08-31 18:54 UTC - T60 implemented and opened as PR #81. Added an optional explicit, bounded, descriptor-safe JSONC read for current opencode routing; separated current/next-run machine routing from each repository's timestamped last-run snapshot on the dashboard and `/how`; and documented configuration, limits, and drift semantics while preserving text-only rendering and legacy peer compatibility. Verification: `bun test` (915 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 0 blocking / 1 minor / 1 invalid; 1 fixed; shipped standard (planned standard). The confirmed atomic config-replacement race was closed with post-read path identity checks and deterministic regression coverage; the default-route fallback finding was verifier-rejected against the existing conservative missing-role contract. Re-panel and verifier returned CLEAR.
- 2026-08-31 19:06 UTC - T60 PR #81 final review tally for head `44ff922618bccb0ce4a3ea1669b0022b32c2e54c` (gatheredAt `2026-08-31T19:06:48Z`): Codex 1 round, verifier-classified 0 blocking / 2 minor / 0 refuted findings, 0 fix pushes; both confirmed minors were parked with thread disposition replies. Claude is not configured (the `claude-code-review.yml` workflow is absent). CI 1 run / 0 reruns. A cursor-bound terminal gather was stable: exact-head CI remained green; reviews 3 (newest 5070171121), issue comments 1 (newest 5483005804), PR-body reactions 0, comment reactions 0, and threads 2. There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 19:08 UTC - T60 merged as PR #81 (squash head `44ff922618bccb0ce4a3ea1669b0022b32c2e54c`). Codex's two verifier-classified minor findings were dispositioned and appended to rolling batch T63; no blocking or refuted findings. Claude is not configured. Recorded external metrics: Codex 1 round, 0 blocking / 2 minor / 0 refuted, 0 fix pushes; CI 1 run / 0 reruns.
- 2026-08-31 19:32 UTC - T61 implemented and opened as PR #82. Made full provider/model identifiers legible on `/how` with a three-by-two layout below 1500px and wrapped route text; added a responsive repository-summary scroll hint for trailing cost columns; reflowed Current approval facts below 1100px; and converted only policy-valid inline GitHub worklog URLs, including exact PR discussion fragments, into concise safe links while retaining unsafe URLs as literal text. Verification: `bun test src/public/app.test.ts src/public/how.test.ts` (151 pass, 0 fail), `bun test` (918 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 2 rounds; findings 0 blocking / 1 minor / 0 invalid; 1 fixed; shipped standard (planned standard). The confirmed sentence-punctuation defect for otherwise valid branch and repository URLs was fixed with end-to-end regressions; both re-review lenses and the adversarial verifier returned CLEAR.
- 2026-08-31 19:45 UTC - T61 review tally for PR #82 (gatheredAt 2026-08-31T19:44:45Z): Codex 1 round, findings 0 blocking / 1 minor / 0 refuted, fix pushes 0; Claude workflow is not configured; CI 1 run / 0 reruns. The exact-head `test` check passed. The verifier classified Codex's trailing-worklog-URL-punctuation finding as minor and it was dispositioned in https://github.com/MrZoller/factory-ui/pull/82#discussion_r3897778197 for the rolling parked-minors batch; no minors-only push was made. Final paginated reviews, issue comments and their reactions, PR-body reactions, and review thread sweep were stable on head 95516146fad404227c7f813a4c43ae15110a458c.
- 2026-08-31 19:46 UTC - T61 merged as PR #82 (squash head 95516146fad404227c7f813a4c43ae15110a458c). CI passed; Codex completed one exact-head review with one verifier-classified minor, parked with a disposition reply and added to rolling batch T63. Recorded external metrics: Codex 1 round, 0 blocking / 1 minor / 0 refuted, 0 fix pushes; CI 1 run / 0 reruns. T63 reached the batching threshold and is now runnable.
- 2026-08-31 20:11 UTC - T62 implemented and opened as PR #83. Qualified every queue, repository-detail, answer-review, and lifecycle identity as `repository/Qn`, adding the machine prefix only when same-named repositories are visible across machines; kept repository-bearing hash links aligned and all source names text-only. Verification: `bun test src/public/app.test.ts` (140 pass, 0 fail), `bun test` (920 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Panel: 2 rounds; findings 0 blocking / 2 minor / 0 invalid; 2 fixed; shipped standard (planned standard). The confirmed stale removed-machine qualification edge case and missing pending/rejected lifecycle regressions were fixed; both re-review lenses and the adversarial verifier returned CLEAR.
- 2026-08-31 20:26 UTC - T62 PR #83 final review tally for head `c90d6491efcb4bd5f2ba947be55c446912b90093` (gatheredAt `2026-08-31T20:26:03Z`): Codex 1 round, verifier-classified 0 blocking / 1 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. The confirmed invisible edit-only draft qualifier finding was dispositioned and parked for rolling batch T63 at https://github.com/MrZoller/factory-ui/pull/83#discussion_r3898079125; no minors-only push was made. A cursor-bound terminal gather was stable: exact-head CI remained green; reviews 2 (newest 5070847386), issue comments 1 (newest 5483925523), PR-body reactions 0, comment reactions 0, and threads 1 / 2 comments (newest PRRC_kwDOT5pyeM7oV_eV). There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 20:27 UTC - T62 merged as PR #83 (squash head `c90d6491efcb4bd5f2ba947be55c446912b90093`). Codex delivered one exact-head review with one verifier-classified minor, parked in rolling batch T63 with a disposition reply; no blocking or refuted findings. Claude is not configured. Recorded external metrics: Codex 1 round, 0 blocking / 1 minor / 0 refuted, 0 fix pushes; CI 1 run / 0 reruns.
- 2026-08-31 20:30 UTC - T63 reclassified from trivial to standard before implementation because eight review fixes span graph rendering, discovery filesystem hardening, routing contracts, URL parsing, and answer-draft identity behavior, necessarily exceeding the trivial line budget and touching existing interfaces. The approved spec and plan satisfy the standard gate; scope remains the eight parked review minors.
- 2026-08-31 21:01 UTC - T63 implemented and opened as PR #84. Resolved eight parked review minors across unavailable/truncated dependency graph states, cheap request rejection and retained code-root identity during discovery, current-versus-legacy routing semantics, aligned browser/server routing validation, bounded repeated worklog URL punctuation, and stale edit-only answer-draft qualification. Verification: `bun test` (927 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check main...HEAD` (passed). Panel: 2 rounds; findings 1 blocking / 1 minor / 0 invalid; 2 fixed; shipped standard (planned trivial). The confirmed unauthenticated answer-route discovery path and outcome-route regression-test gap were both fixed; the re-review correctness lens returned CLEAN and the final behavior remained fully verified.
- 2026-08-31 21:10 UTC - T63 PR #84 final review tally for head `cf5acb11d765c5019d5d040cde8f5414a2abe5ef` (gatheredAt `2026-08-31T21:10:24Z`): Codex 1 round, verifier-classified 0 blocking / 0 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 1 run / 0 reruns. Codex's exact-head completed summary and attributable clean PR-body +1 are its verdict. A cursor-bound terminal gather was stable: exact-head CI remained green; reviews 0, issue comments 1 (newest 5484509436), PR-body reactions 1 (newest 480803994), comment reactions 0, and threads 0. There is no hold label and the verified local task branch matches the remote head.
- 2026-08-31 21:11 UTC - T63 merged as PR #84 (squash `aee3ba93a7badf35f6984ccb1391bcca914c8936`). CI passed on head `cf5acb11d765c5019d5d040cde8f5414a2abe5ef`; Codex delivered one clean exact-head review round, with no blocking, minor, or refuted findings. Claude is not configured. Recorded external metrics: Codex 1 round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes; CI 1 run / 0 reruns.
- 2026-08-31 21:13 UTC - No runnable task remained after answer reconciliation and sync: every plan task is complete, no task is mid-flight, and no open or answered-unconsumed question remains. Set the factory phase to `idle`. Verification: `factory-answers settle` and `factory-answers prepare --json` both returned `idle`; inspected `.factory/plan.md`, `.factory/state.json`, and `.factory/questions.md`; no product tests were needed for bookkeeping-only changes.
- 2026-09-01 02:12 UTC - Approved plan for backlog tasks T64-T69 (operator, Chris's explicit gate approval in the operator session). `/approve plan` remains broken upstream (opencode-factory#103, repo-relative helper); settle-equivalent verified by operator: `factory-answers pending-count` = 0. Set `plan_approved: true`, phase `build`.
- 2026-09-01 02:50 UTC - T64 implemented and opened as PR #92. Added the explicit `answerAuth: "tailnet-open"` browser opt-in while preserving secret authentication by default and the mandatory server-to-helper secret; advertised intake mode, removed answer-route CORS grants, kept strict JSON/idempotency request-shape checks, omitted open-mode browser credential UI and headers, and linked peer questions to their same-origin owning dashboard. Reclassified standard to major because removing browser authentication is a security-boundary change, so the PR is held for human merge authority. Verification: `bun test` (934 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed), and `git diff --check` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped major (planned standard). Both review lenses returned CLEAN and the adversarial verifier returned CLEAR.
- 2026-09-01 03:04 UTC - T64 PR #92 shepherd pass: verifier classified the owner-reported insecure-context UUID failure as blocking and Codex thread `discussion_r3900358418` as minor. Fixed the blocking finding in `945ef38` by using a `crypto.getRandomValues` v4 UUID fallback and placing idempotency-key generation inside submission error handling; verification: `bun test src/public/app.test.ts` (148 pass, 0 fail), `bun run lint` (Prettier and strict TypeScript passed); pushed fix for owner finding: `945ef38`. Replied on the PR. Parked the Codex minor with a disposition reply; no minors-only push.
- 2026-09-01 03:20 UTC - T64 PR #92 final review tally for head `945ef3837970565af67f241fcb0f568ae3f2e2c4` (gatheredAt `2026-09-01T03:20:17Z`): Codex 2 rounds, verifier-classified 0 blocking / 3 minor / 0 refuted findings, 0 fix pushes; Claude is not configured (the `claude-code-review.yml` workflow is absent); CI 2 runs / 0 reruns. The three confirmed Codex minors have disposition replies and are parked for the rolling review-minors batch: unauthenticated cross-site outcome reads (https://github.com/MrZoller/factory-ui/pull/92#discussion_r3900358418), Host validation in tailnet-open mode (https://github.com/MrZoller/factory-ui/pull/92#discussion_r3900487725), and peer lifecycle migration (https://github.com/MrZoller/factory-ui/pull/92#discussion_r3900488016). A cursor-bound terminal gather was stable: exact-head CI remained green; reviews 5 (newest 5073703542), issue comments 3 (newest 5488278688), PR-body reactions 0, comment reactions 0, and threads 3. The verified local task branch matches the remote head; T64 remains held because it is major and awaits operator merge authority.

- 2026-09-01 03:24 UTC - T64 PR #92 was merged by operator authority (squash `58b8c65940dc1afc390e8047de1b71d2389bb1bf`). Post-merge bookkeeping marked T64 complete, cleared tracked PR state and hold, and recorded the three verifier-classified Codex minors in new rolling batch T70. External review metrics: Codex 2 rounds, 0 blocking / 3 minor / 0 refuted, 0 fix pushes; Claude not configured; CI 2 runs / 0 reruns.
