{
  "name": "factory-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "lint": "prettier --check . && tsc --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "happy-dom": "^20.11.2",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
